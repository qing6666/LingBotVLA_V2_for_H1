# LingBot-VLA 2.0 复现全流程指南(架构视角)

> 本文档从"架构"出发梳理复现全流程,核心是讲清楚:**项目管什么、环境管什么、两者在哪接、换环境时改什么**。看懂这份,你就具备把项目迁移到任何机器人/仿真的能力。

---

## 一、核心认知:大脑 / 身体 / 接口

整个系统分三块,务必先建立这个认知:

```
   【身体/环境侧】(每个环境不同,你写/换)    【大脑/项目侧】(通用,不动)
   相机 + 机器人                              模型 + 训练 + 推理服务
        │                                          ▲
        └──────────► 固定接口(数据契约)──────────┘
                     观测dict / 动作dict
```

| | 是什么 | 谁管 | 固定吗 |
|---|---|---|---|
| **大脑(项目)** | 训练 + 推理服务 | 项目代码 | ✅ 通用不动 |
| **身体(环境)** | 相机控制 + 机器人控制 | 你写/集成 | ❌ 每个环境不同 |
| **接口** | 观测/动作的数据格式 | 写死在代码里 | ✅ 固定契约 |

> **一句话**:项目是个"纯大脑",它**不碰任何硬件**。相机/机器人控制都在"环境侧",通过**固定接口**和大脑对接。

---

## 二、全流程三阶段:谁管什么

```
┌──────────────────────────────────────────────────────────────────┐
│ 阶段1:数据采集        阶段2:训练          阶段3:推理执行        │
│ ─────────────        ──────────          ──────────────        │
│ 【环境侧】            【项目侧】           【环境侧+项目侧】      │
│ · 相机控制(读图)    · compute_norm     【项目】推理服务(deploy) │
│ · 机器人控制(遥操)  · train            【环境】runner客户端    │
│      ↓                   ↓                  · 相机控制(实时读) │
│  LeRobot数据集 ────► 模型checkpoint ──►   · 机器人控制(执行)  │
│                                              ↓                   │
│                                          机器人动作               │
└──────────────────────────────────────────────────────────────────┘
```

**关键:阶段2(训练)完全在项目内部,不碰硬件。硬件控制只在阶段1(数采)和阶段3(推理 runner)。**

---

## 三、固定接口(迁移的核心!)

不管什么环境,只要遵守这个**数据契约**就能接上项目:

### 接口①:观测(环境 → 大脑)
```python
{
  "image":  {"camera1": 左腕RGB, "camera2": 右腕RGB, "camera3": 头部RGB},
  "state":  np.array([12维关节]),
  "prompt": ["任务指令"],
}
```

### 接口②:动作(大脑 → 环境)
```python
{
  "action.arm.position":      [...],   # 臂关节(真实弧度)
  "action.effector.position": [...],   # 夹爪
}
```

> **迁移本质**:你的环境能"产出①、执行②",就能接上。大脑(项目)一字不改。

---

## 四、各文件/代码的角色(别混淆!)

| 文件/代码 | 角色 | 你要做的 |
|---|---|---|
| `configs/robot_configs/<name>.yaml` | **翻译规则**:把你的字段切片映射到统一特征 | 每个机器人配一份(SO101→`so101.yaml`) |
| `assets/norm_stats/<name>.json` | **归一化统计** | 每个数据集重算一份(`compute_norm_stats`) |
| `configs/vla/.../<name>.yaml` | **训练配置** | 指向你的 robot_config + 权重路径 |
| `deploy/websocket_client_policy.py` | **通用收发工具**(电话) | ❌ **不改**(任何环境都用它) |
| `deploy/<name>_runner.py` | **环境控制循环**(你写) | ★ 相机SDK+机器人SDK+调client |
| `deploy/websocket_policy_server.py` | 推理服务端 | ❌ 不改 |
| `deploy/lingbot_vla_v2_policy.py` | 推理逻辑(apply/去噪/unapply) | ❌ 不改 |

> **robot_config 是"翻译规则",不是接口本身**。接口是代码里的数据格式(第三节),yaml 只是告诉项目"你的字段怎么映射成统一特征"。

---

## 五、SO101 落地:配置 + 命令

### 数据情况
- `dual_arm_dataset`(LeRobot,61 episode,55558帧,fps30)
- 12维 state/action = `[左臂6(含左夹爪), 右臂6(含右夹爪)]`
- 相机:camera1=左腕D405, camera2=右腕D405, camera3=头部D435i
- task 文本已存在 ✅

### 第0步:robot_config(已创建)
[`configs/robot_configs/so101.yaml`](configs/robot_configs/so101.yaml):12维拆 arm(10)+effector(2),3相机重命名。

### 第1步:训练配置
```bash
cp configs/vla/real_robot/real_robot.yaml configs/vla/real_robot/so101.yaml
```
改 data 段(data_name=so101, joints=arm14/end14/eff2, norm_type=meanstd)+ train.align_params={} (初次关蒸馏) + model 路径。

### 第2步:算归一化统计
```bash
bash train.sh scripts/compute_norm_stats.py ./configs/vla/real_robot/so101.yaml \
    --data.data_name so101 \
    --data.train_path /home/mjq/robot_item/lingbot-vla-v2-main/dual_arm_dataset \
    --data.norm_path assets/norm_stats/so101.json
```

### 第3步:训练
```bash
bash train.sh tasks/vla/train_lingbotvla.py ./configs/vla/real_robot/so101.yaml \
    --data.data_name so101 \
    --data.train_path /home/mjq/robot_item/lingbot-vla-v2-main/dual_arm_dataset \
    --data.norm_stats_file assets/norm_stats/so101.json \
    --train.output_dir output/so101
```

### 第4步:开环评估
```bash
python scripts/open_loop_eval.py \
    --model_path output/so101/你的checkpoint --robo_name so101 \
    --data_path /home/mjq/robot_item/lingbot-vla-v2-main/dual_arm_dataset --use_length 50
```

### 第5步:推理部署
```bash
# 服务端(GPU)
export QWEN3VL_PATH=/你的路径/Qwen3-VL-4B-Instruct
python -m deploy.lingbot_vla_v2_policy --model_path output/so101/ckpt --use_length 25 --port 8761

# 客户端(SO101侧,见第六节 runner)
python deploy/so101_runner.py
```

---

## 六、控制代码在哪写(★ 关键)

### 阶段1 数采:**不在本项目**
用 LeRobot 官方采集脚本(内置相机+机器人控制),产出 LeRobot 数据集。

### 阶段3 推理:**新建 runner**(不塞进 websocket_client_policy!)
`deploy/websocket_client_policy.py` 是**通用收发工具(电话),保持不动**。控制代码写在新文件:

```python
# deploy/so101_runner.py(你新建)
from deploy.websocket_client_policy import WebsocketClientPolicy   # 通用工具,不改
import pyrealsense2 as rs                                          # 相机SDK
# import 机器人SDK(lerobot so101 等)

client = WebsocketClientPolicy(host="GPU服务器IP", port=8761)     # 拿起"电话"
robot = 机器人SDK.connect(...)
cams  = {i: 相机SDK.connect(id) for i,id in enumerate([左,右,头])}

while True:
    # —— 环境控制:读相机 + 读状态(写在 runner)——
    images = {f"camera{i+1}": cam.read() for i,cam in cams.items()}
    obs = {"image": images, "state": robot.read_state(), "prompt": ["The dual arms flip..."]}
    # —— 调通用工具收发 ——
    action = client.infer(obs)
    # —— 环境控制:执行 ——
    robot.send_action(action)
```

> **类比**:websocket_client_policy = 电话(通用,不改);runner = 打电话的人(环境特定,你写)。

---

## 七、迁移能力(换环境改什么)

因为接口固定,**换环境只改"身体侧",大脑(项目)不动**:

| 换的场景 | 改什么 | 项目核心 |
|---|---|---|
| **换真机**(SO101→别的臂) | robot_config + norm_stats + 数据 + runner(新SDK) | ❌ 不动 |
| **换仿真**(真机→RoboTwin) | robot_config + 数据(仿真采)+ runner(仿真API) | ❌ 不动 |
| **换相机** | runner 相机SDK + robot_config images 映射 | ❌ 不动 |
| **换任务** | 数据 + norm_stats | ❌ 不动 |

> 参考:项目里 [`experiment/robotwin/`](experiment/robotwin/) 就是"把 RoboTwin 仿真接上项目"的范例。

---

## 八、总图(记住这张就懂)

```
     【身体/环境侧】(换)               【大脑/项目侧】(不动)
     ────────────────                 ────────────────

  真机/仿真/别的机器人
     │ 相机SDK + 机器人SDK
     ▼
 ┌──────────┐  观测①   ┌────────────────┐
 │  runner  │ ───────► │ 推理服务(deploy)│
 │ (你写)   │          │  模型(大脑)    │
 │ 读相机/状态│ ◄─────── │                │
 └──────────┘  动作②   └────────────────┘
     │ 执行                    ▲
     ▼                         │ 训练产出
  机器人动                 ┌─────────┐
                           │ 训练    │ ← 数据(环境采的)
                           └─────────┘

 配置(身体侧适配接口):
 · robot_config(翻译规则,每机器人一份)
 · norm_stats(归一化,每数据集一份)
 · runner(环境控制,每环境一份)

 ★ 换环境 = 换身体侧(robot_config+norm_stats+数据+runner),大脑不动
 ★ websocket_client_policy(通用电话)永远不改
```

---

## 九、检查清单

- [ ] 权重下载(LingBot预训练 + Qwen3-VL)
- [ ] `configs/robot_configs/so101.yaml`(字段映射)✅ 已创建
- [ ] `configs/vla/real_robot/so101.yaml`(训练配置:data+关align_params+权重路径)
- [ ] task 文本已确认 ✅
- [ ] 跑 `compute_norm_stats` → `so101.json`
- [ ] 跑训练 → checkpoint
- [ ] 跑 `open_loop_eval` 验证
- [ ] 起 deploy 服务端
- [ ] 写 `deploy/so101_runner.py`(相机SDK+机器人SDK+client)+ 跑客户端

---

## 十、一句话总结

> **这个项目是"平台无关的大脑"**(训练 + 推理服务,通用不动)。
> **你要做的是给它在每个环境配一个"身体"**:① robot_config(翻译规则)② norm_stats(归一化)③ runner(相机/机器人控制)。
> **三者通过固定接口(观测/动作 dict)接上大脑**。换机器人/仿真 = 换这三个"身体侧"配置,**大脑一字不改**——这就是算法迁移能力。
