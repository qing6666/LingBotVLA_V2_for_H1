# LingBot-VLA 2.0 训练核心技术点详解

> **对应代码**:[tasks/vla/train_lingbotvla.py](tasks/vla/train_lingbotvla.py)(训练主循环)
> **本文目的**:用「数字例子 + 生活类比 + 逻辑框图」把训练主循环里最核心的 5 个技术点讲透,可作为复习/调试参考。

---

## 总览

| # | 技术点 | 一句话 | 核心位置 |
|---|---|---|---|
| 1 | **梯度累积** | 显存装不下大 batch,分几次攒梯度再更新 | [L803](tasks/vla/train_lingbotvla.py#L803) / [L953](tasks/vla/train_lingbotvla.py#L953) |
| 2 | **多 loss 加权合成** | 模型同时学动作+深度+未来+均衡,加权打总分 | [L853](tasks/vla/train_lingbotvla.py#L853) |
| 3 | **depth/video 双查询蒸馏** | 教师网络示范,学生模仿(LingBot 2.0 创新) | [L446](tasks/vla/train_lingbotvla.py#L446) / [L822](tasks/vla/train_lingbotvla.py#L822) |
| 4 | **MoE 负载均衡** | 三重机制防专家忙闲不均 | [L588](tasks/vla/train_lingbotvla.py#L588) |
| 5 | **断点续训 + 双格式 checkpoint** | 存档/读档,DCP 续训 + HF 推理 | [L711](tasks/vla/train_lingbotvla.py#L711) / [L1178](tasks/vla/train_lingbotvla.py#L1178) |

---

## 技术点 1:梯度累积(Gradient Accumulation)

### 核心矛盾
- **想要的**:一次用 batch=4 个样本算梯度再更新(梯度稳,效果好)
- **现实**:显存只够塞进 micro_batch=1 个样本

### 解决办法:把"一次 4 个"拆成"四次 1 个,梯度累加"

以样本 A/B/C/D 为例,某参数的各样本梯度记为 `g_A, g_B, g_C, g_D`:

| 步骤 | 做什么 | 参数里存的梯度 |
|---|---|---|
| ① | 前向 A → 反向 | `g_A` |
| ② | 前向 B → 反向(**不清零!**) | `g_A + g_B` |
| ③ | 前向 C → 反向 | `g_A + g_B + g_C` |
| ④ | 前向 D → 反向 | `g_A + g_B + g_C + g_D` |
| ⑤ | `optimizer.step()` 更新 | 用总和更新一次 |
| ⑥ | `zero_grad()` 清零 | 准备下一轮 |

> PyTorch 默认反向后梯度是**累加**(不是覆盖),所以天然支持这个。

### 为什么 loss 要 `÷ len(micro_batches)`?
- batch=4 一次算用**平均**梯度:`(g_A+g_B+g_C+g_D) / 4`
- 梯度累积攒的是**总和**:`g_A+g_B+g_C+g_D`(大了 4 倍,会训崩)
- 所以每个样本反向前先 ÷4,累加后正好等于平均 ✅

### 代码
```python
for micro_batch in micro_batches:        # ①②③④ 循环 4 次
    ...
    loss = loss / len(micro_batches)     #   每个 loss 先 ÷4  ← 关键
    loss.backward()                      #   反向,梯度累加(不清零)

optimizer.step()                         # ⑤ 循环外,更新一次
optimizer.zero_grad()                    # ⑥ 清零
```

### 生活类比
一次只能搬 1 块砖,想要"搬 4 块"的效果 → 搬 1 块记下用力方向,搬 4 次把方向**加起来**,但每次记账要**打 ¼ 折**(÷4),否则用力猛 4 倍会闪腰。攒满 4 次调整一次姿势(`step`),清空账本(`zero_grad`)。

---

## 技术点 2:多 loss 加权合成

### 为什么有多个 loss
VLA 模型不止学动作,还要学:

| loss 分量 | 学什么 | 性质 |
|---|---|---|
| `vla_loss` | 动作预测(flow matching) | **主科** |
| `depth_loss` | 当前深度理解 | 副科(蒸馏) |
| `future_depth_loss` | 未来深度 | 副科(蒸馏) |
| `future_video_loss` | 未来视频(DINO) | 副科(蒸馏) |
| `seq_wise_loss` | MoE 专家均衡 | 纪律分 |
| `router_z_loss` | router 数值稳定 | 纪律分 |

### 合成公式
```
总 loss = 1.0   × vla_loss          (动作,主科)
        + 0.004 × depth_loss        (深度,副科)
        + 0.004 × future_depth_loss
        + 0.004 × future_video_loss
        + 系数  × seq_wise_loss     (MoE 均衡)
        + 系数  × router_z_loss
```
> 加权在**模型内部**完成,返回的 `loss` 已是总分,直接 `backward()`。其余分量只用于日志。

### 数字例子:为什么 depth 权重只有 0.004?
```
vla_loss=2.0, depth_loss=5.0, video_loss=3.0
总 loss = 1.0×2.0 + 0.004×5.0 + 0.004×3.0
        = 2.0    + 0.02       + 0.012
        = 2.032
```
总分几乎全由 vla_loss(2.0)决定,depth/video 加起来才占 1.5%——**副科不能抢主科风头**,否则模型光顾着拟合深度,动作反而学烂。

### 生活类比
学生考 6 门:语文(动作)是主科满分制;美术体育(深度视频)是副科权重 0.004;还有纪律分(MoE 均衡)。总成绩=加权和,学校故意压低副科权重,让学生主攻主科。

---

## 技术点 3:depth/video 双查询蒸馏(Dual-Query Distillation)

### 什么是"蒸馏"
一个强的**教师**模型,把"本事"教给**学生**模型。学生**模仿教师的中间特征(思路)**,而非死记答案。

### 为什么 VLA 需要
学生模型(Qwen3-VL)擅长看图动手,但有两块短板:
- 不懂 **3D 几何**(看不出远近深浅)
- 不懂 **时间动态**(预测不了下一秒)

请两位专科教师补课:
| 教师 | 擅长 | loss |
|---|---|---|
| MoGe + MoRGBD | 单图深度估计 | `depth_loss` |
| video-DINO | 视频时序/未来预测 | `future_video_loss` |

### 教师学生互动框图
```
          ┌──── 同一张图 ────┐
          ▼                  ▼
   【教师】(冻结,no_grad)  【学生 VLA】(训练)
        │                      │
        ▼ 目标特征              ▼ 预测特征
   target=[0.3,-0.1,..]   pred=[0.1,0.2,..]
        │                      │
        └────► 算 MSE ◄────────┘
                 │
                 ▼ backward
       梯度只更新学生(教师 no_grad 不动)
```

### `no_grad` 的含义
`with torch.no_grad():` 包住教师前向 = "**不记录梯度**"。后果:
- 教师输出是**固定常数**(不参与反向)
- 教师参数**永远不变**(冻结)
- 梯度**只流给学生**

> 教师是"标准答案提供者",自己不学习。

### 数字例子
```
教师 target : [0.30, -0.10, 0.80, ...]
学生 pred   : [0.10,  0.20, 0.50, ...]
loss = MSE  = ((0.3-0.1)² + (-0.1-0.2)² + (0.8-0.5)² + ...) / 256
```
训练几万步后,学生特征逼近教师 → 学生也"懂"了几何和时序。因为学的是"思路"不是"答案",能泛化到新场景。

### 为什么叫"双查询 Dual-Query"
学生在 VLM token 后**额外插两个 query token**:一个查当前深度(→MoRGBD),一个查未来视频(→DINO)。"Dual" = 这两个查询。

### 代码
```python
# 建教师(阶段③)
moge_model, morgbd_model = build_depth_model(...)      # L446
video_teacher = build_video_model(...)                 # L455

# 训练循环内:教师算目标(no_grad)
with torch.no_grad():                                   # L815
    depth_targets = get_depth_target(...)               # L822
    future_video_targets = get_video_target(...)        # L827

# 传给学生,内部算蒸馏 loss
model_outputs = model(..., depth_targets=..., future_video_targets=...)  # L853
```

### 生活类比
学生语文好但数学差。请两位固定的高手家教(no_grad,自己不学),每次演示"正确思路"(特征向量)。学生模仿思路,慢慢有了数学直觉——学的是思维方式不是背答案,换题也会。

---

## 技术点 4:MoE 负载均衡(三道保险)

### MoE 是什么
把一层的大 FFN 换成 **32 个小专家 + 1 个路由器**:
```
每个 token → 路由器打分 → 选 top-k(如32选4)个专家处理
```
**好处**:参数多(容量大),但每个 token 只激活 4 个 → 算得少。

### 问题:路由器会"偏心"
```
100 个 token,理想每个专家收 ~12 个
实际偏心:专家0 收 50(累死),专家1 收 0(闲死)
```
**后果**:闲专家浪费显存、模型退化成小模型、训练不稳。

### 三道保险

#### 保险①:loss-free bias(无损偏置)—— 主力
**思路**:不动 loss,悄悄给路由器打分**加偏置**。
```
每步 step 前(pre-hook)统计各专家负载:
  专家0 负载 50(忙)→ bias = -0.5(压低分数,少往这送)
  专家1 负载 0 (闲)→ bias = +0.5(抬高分数,吸引 token)
```
**为什么"无损"**:调的是路由器输入偏置,不加 loss 惩罚,**不污染主任务梯度**(DeepSeek-V3 方案)。

#### 保险②:sequence_wise_loss(辅助 loss)
直接加 loss 项,惩罚"一个序列内专家分配不均",加进总分(系数 `sequence_wise_loss_coeff`)。和保险①互补:①是数值调度,②是梯度施压。

#### 保险③:router_z_loss(数值稳定)
惩罚 router 的 logits 平方和,防 softmax 数值爆炸/梯度消失。纯数值健康。

### 数字例子:负载怎么被拉平
```
第 1 步:负载=[50, 0, 30, 5, ...]   MaxVio 高
        ↓ ①给专家0加-0.5、专家1加+0.5;②产生惩罚loss
第 2 步:负载=[35, 8, 25, 10, ...]  好转
第 N 步:负载=[13, 11, 12, 12, ...]  MaxVio≈0,专家都干活
```
日志 **`MaxVio`**(最大违反度)= 体检指标,越小越均衡。

### 代码
```python
# 保险①:loss-free bias,注册到 step 前
_lb_hook = build_moe_load_balance_hook(model, coeff=bias_update_speed, ...)  # L588
optimizer.register_step_pre_hook(_lb_hook)        # L592

# 保险③:z_loss
router_z_loss = loss_log.get("router_z_loss", ...)  # L888

# 保险②:seq_wise_loss(model 内算,合进总 loss)
```

### 生活类比
车间 32 个师傅,调度员(路由器)决定工件送哪 4 个。调度员偏心→名师累瘫、其他喝茶。
- ① 主任每天**悄悄改调度员偏好表**(忙的少派、闲的多派),不改工件标准(无损)
- ② 批次内全挤某师傅→**扣绩效**
- ③ 防调度员打分太极端(100分/0分),要求**打分温和**

---

## 技术点 5:断点续训 + 双格式 checkpoint

### 断点续训:恢复**全部**状态(不止权重)
```python
Checkpointer.load(cp, state, ...)                    # L711
state = {"model", "optimizer", "extra_state": {
    "global_step", "lr_scheduler",
    "train_dataloader",   # ← 连数据进度都恢复(不重复不漏)
    "environ_meter", "torch_rng_state"   # ← 随机种子也恢复
}}
```
从断点"无缝继续",像没断过。

### 双格式存档
```
Checkpointer.save(...)                # L1178  DCP 格式 → 下次续训用(含 optimizer 等训练态)
save_hf_checkpoint_best_effort(...)   #        异步转 HF 格式 → deploy 推理直接加载(只含权重)
```

### 生活类比
游戏存档不只存"等级(权重)",还存"背包、任务进度、下一步去哪、骰子点数",读档后完全接着玩。还存两份:完整存档(DCP)给自己,精简存档(HF)转给后厨(推理)。

---

## 五个技术点如何协作(一个 step 内同时发生)

```
取 N 个 micro_batch ──(①梯度累积)──────────────────┐
每个 micro_batch:                                     │
  ├─ 教师算 depth/video 目标 ──(③蒸馏)               │
  ├─ 学生 model() 前向:                               │
  │    动作loss + 深度蒸馏loss + 视频蒸馏loss          │
  │    + MoE均衡loss ──(②多loss合成 + ④MoE均衡)      │
  │    → 合成总 loss                                   │
  └─ loss/N → backward(梯度累积)                     │
                                                       ▼
                          optimizer.step 前:MoE bias 自动调(④)
                          optimizer.step → 更新参数
                          zero_grad
                          到 save_steps → 存 checkpoint(⑤)
```

- **①梯度累积**:显存小也能用大 batch
- **②多 loss**:把多个目标打包成一个总分
- **③蒸馏**:让模型不只动手,还懂几何/时序
- **④MoE 均衡**:让 32 个专家都出力,容量用满
- **⑤checkpoint**:训练可断点续训,产物可直接部署

---

## 核心代码位置速查

| 技术 | 关键代码 | 行号 |
|---|---|---|
| 梯度累积循环 | `for micro_batch in micro_batches` | [L803](tasks/vla/train_lingbotvla.py#L803) |
| loss 均摊 | `loss = loss / len(micro_batches)` | [L882](tasks/vla/train_lingbotvla.py#L882) |
| 反向 | `loss.backward()` | [L892](tasks/vla/train_lingbotvla.py#L892) |
| 更新 | `optimizer.step()` | [L953](tasks/vla/train_lingbotvla.py#L953) |
| 多 loss 解包 | `model_outputs = model(...)` | [L853](tasks/vla/train_lingbotvla.py#L853) |
| 建深度教师 | `build_depth_model(...)` | [L446](tasks/vla/train_lingbotvla.py#L446) |
| 建视频教师 | `build_video_model(...)` | [L455](tasks/vla/train_lingbotvla.py#L455) |
| 教师算目标 | `get_depth_target / get_video_target` | [L822](tasks/vla/train_lingbotvla.py#L822) |
| MoE bias hook | `build_moe_load_balance_hook` + `register_step_pre_hook` | [L588](tasks/vla/train_lingbotvla.py#L588)/[L592](tasks/vla/train_lingbotvla.py#L592) |
| z_loss | `router_z_loss = loss_log.get(...)` | [L888](tasks/vla/train_lingbotvla.py#L888) |
| 断点恢复 | `Checkpointer.load(...)` | [L711](tasks/vla/train_lingbotvla.py#L711) |
| 存 checkpoint | `Checkpointer.save(...)` | [L1178](tasks/vla/train_lingbotvla.py#L1178) |

---

## 三条支线:训练 / 推理完整详解(动作 + 深度蒸馏 + 视频蒸馏)

### 全貌:3 支线共享一次 VLM 前向

```
   输入:图像 + 语言 + 状态 + (训练还有:真实动作、深度目标、视频目标)
                        │
                        ▼
        ┌─────────────────────────────┐
        │  VLM + MoE 联合前向(只走 1 次)│   ← 关键:一次前向,产出三路表征
        └──────────────┬──────────────┘
                       │ outputs_embeds / suffix_out
          ┌────────────┼────────────┐
          ▼            ▼            ▼
    ① 动作支线    ② 深度支线     ③ 视频支线
    action expert  depth query    video query
       │             │              │
    预测速度 v_t   预测深度       预测未来帧特征
       │             │              │
    vs u_t        vs depth_target  vs video_target  ← 教师算的"标准答案"
       │             │              │
    vla_loss      depth_loss     future_video_loss
```

> 动作答案是**人类示教**给的;深度/视频答案是**教师网络**算的(train_lingbotvla 里 `no_grad` 算好传进来)。

### 训练(7 环,3 支线全开)— `FlowMatchingV2.forward` + `Policy.forward`

| 环 | 做什么 | 代码 |
|---|---|---|
| 0 准备输入 | 传入 actions + depth_targets + future_video_targets | train_lingbotvla L822/827/853 |
| 1 嵌入前缀 | 图像+语言 → token,**插入 depth/video query**(双查询) | `embed_prefix` L803 |
| 2 Flow Matching 造样本 | x_t=t·noise+(1-t)·actions;u_t=noise-actions(**只服务支线①**) | L787-794 |
| 3 嵌入后缀 | state+x_t+time → suffix | `embed_suffix` L810 |
| 4 VLM+MoE 前向 | **一次前向**得 outputs_embeds / suffix_out / router_logits | `qwenvl_with_expert.forward` L833 |
| 5 三支线算 loss | ①v_t vs u_t→vla ②depth_pred vs target→depth ③video_pred vs target→video | L900/847/854 |
| 6 MoE 辅助 loss | seq_wise_loss + router_z_loss | `_moe_losses_and_metrics` L913 |
| 7 合成+反向 | total=vla+depth+future_depth+future_video+seq_wise+z;backward | `Policy.forward` L1305 |

### 推理(4 环,只走动作支线,享蒸馏红利)— `sample_actions` + `predict_velocity`

| 环 | 做什么 | 代码 |
|---|---|---|
| 1 前缀前向+缓存 | images+lang → KV cache(只算 1 次);query token 仍在结构里但不取输出 | L943-969 |
| 2 纯噪声出发 | x_t=randn;time=1.0 | L972-973 |
| 3 去噪循环 N 步 | predict_velocity(复用 KV)→v_t;x_t+=dt·v_t(**只动作支线**) | L987-1000 |
| 4 返回动作 | x_t→动作;unapply 反归一化→机器人 | L1002 |

### 关键洞察:训练 3 路监督 vs 推理 1 路输出

| | 训练 | 推理 |
|---|---|---|
| 动作支线 | ✅ flow matching 学方向 | ✅ 去噪生成动作 |
| 深度支线 | ✅ 对齐教师深度 | ❌ 不算(query 在结构里) |
| 视频支线 | ✅ 对齐教师未来 | ❌ 不算(query 在结构里) |
| VLM 前向 | 1 次 | 1 次(缓存)+N 次后缀 |
| 教师网络 | ✅ 在跑(no_grad 算 target) | ❌ 不需要 |

**为什么训练开 3 路、推理只 1 路**:蒸馏是训练期的"素质课",把 VLM 表征教得更好(懂立体、懂未来);推理时表征已定型,只取动作,但动作是被三路监督熏陶过的 VLM 产出 → 更准更泛化。

---

## 教师网络详解:深度 + 视频 target 怎么算

### 教师在哪、干什么
两个教师都是**预训练好、冻结的**,训练时在 `no_grad` 下算"标准答案"(target)传给学生对照:
```python
# train_lingbotvla.py
moge_model, morgbd_model = build_depth_model(...)        # L446 深度教师(两个)
video_teacher = build_video_model(...)                   # L455 视频教师
with torch.no_grad():                                    # L815 教师不算梯度
    depth_targets = get_depth_target(... moge_model, morgbd_model ...)   # L822
    future_video_targets = get_video_target(video_teacher, ...)         # L827
model_outputs = model(..., depth_targets=..., future_video_targets=...) # L853 传给学生
```

### 教师①:深度教师(MoGe + MoRGBD,两阶段接力)
> [get_depth_target](lingbotvla/models/vla/vision_models/module_utils.py#L426) / [build_depth_model](lingbotvla/models/vla/vision_models/module_utils.py#L71)

单个模型要么懂"估深度"(MoGe)、要么懂"看外观",不够;所以两阶段:
```
当前 RGB 图
    │
    ▼ ① MoGe(从 RGB 估深度,2D→3D):moge_model.infer(图) → depth_pred(深度图)
    │
    ▼ ② MoRGBD / LingBot-Depth(RGB+深度融合):morgbd_model.infer_feat(图, depth_pred)
    │
    ▼
depth_target(特征) + cls_token  →  学生的 depth query 对齐
```
**通俗**:MoGe 是"透视眼"(看平面照片估远近),MoRGBD 是"融合器"(把颜色+远近揉成懂场景的特征)。学生学这个"立体感"。

### 教师②:视频教师(video-DINO)
> [get_video_target](lingbotvla/models/vla/vision_models/module_utils.py#L345) / [build_video_model](lingbotvla/models/vla/vision_models/module_utils.py#L334)

吃"当前+未来帧",由 DINO 理解**时序运动**,输出"未来该是什么样"的特征:
```
当前帧 + 未来帧
    │ 拼成视频 [warmup, 当前, 未来](给 DINO 运动上下文)+ ImageNet 归一化
    ▼
video-DINO:get_future_feature(video)
    │
    ▼
patch_target(未来帧特征,主目标)+ cls_target + current_patch(当前特征)
    │
    ▼ → 学生的 video query 对齐
```
**通俗**:video-DINO 是"预言家"(看过去+现在,推断下一秒画面)。学生学这个"预判"。

### 两者共性
| | 深度教师 | 视频教师 |
|---|---|---|
| 模型 | MoGe + MoRGBD | video-DINO |
| 输入 | 当前 RGB 图 | 当前 + 未来帧序列 |
| 输出 target | depth_target(深度融合特征) | patch_target(未来帧特征) |
| 梯度 | no_grad(冻结) | no_grad(冻结) |
| 监督的学生 | VLM 的 depth query | VLM 的 video query |
| 教的"本事" | 几何/立体感(空间) | 时序/预判(时间) |

**两个关键点**:
1. **输出是"特征"不是"像素"** —— 学生学的是"理解方式"不是"画图",所以能泛化。
2. **教师只在训练时出现** —— 推理不加载教师(省算力),学生已"出师"。

### 完整蒸馏数据流
```
当前 RGB ──► MoGe ──► MoRGBD ──► depth_target ──────┐
                                                     │
当前+未来 ──► video-DINO ──► video_target ──────────┤
                                                     │
              传给 model.forward(...)                 │
                     │                                │
                     ▼                                │
  VLM 前向 → depth query ──vs depth_target──► depth_loss
           → video query ──vs video_target──► video_loss
           → action expert ──(flow matching)──► vla_loss
```

### 重要推论:native depth → 推理不需要深度相机
深度 target 是 **MoGe 从 RGB 估的**(不是真深度传感器),学生通过蒸馏**内化**了"从 RGB 推几何"的能力:
```
训练:RGB ─► MoGe 估深度 ─► 蒸馏给学生(学会从RGB理解深度)
推理:RGB ─► 学生自己理解深度 ─► 动作   ← 不需 MoGe、不需深度相机
```
**结论**:部署时**只需普通 RGB 相机**(多视角:顶部+左右腕),**不需要结构光/ToF/双目深度相机**。这正是 "native depth" 的含义——模型自己懂深度,用算法(蒸馏)换掉了昂贵的深度传感器。
> 注意:多视角 RGB 相机(普通彩色)还是要的(提供 images);它 ≠ 深度相机。

---

## 调试时看什么

| 现象 | 看哪里 |
|---|---|
| 训练崩/loss 爆 | `GradNorm`、`router_z_loss`、是否忘 `÷len` |
| 专家偏科 | `MaxVio`、`load_cv`、`moe_expert_load_bar` 直方图 |
| 蒸馏没生效 | `Depth_Loss`/`FutureVideo_Loss` 是否下降、`align/*` 曲线 |
| 动作学不好 | `VLA_Loss` 主曲线、学习率 `LR` |
| 续训失败 | checkpoint 目录、`async_hf_failures.jsonl` |
| 速度慢 | `StepTime`、`Depth_Forward_Time`、profiler |
