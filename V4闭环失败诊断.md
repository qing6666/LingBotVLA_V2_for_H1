# V4 闭环失败诊断 —— 2026-08-29

> 前情:v4 训练 40000 步完成,10 回合闭环 0/10(方块从未被碰)。
> open-loop MAE 0.087 却很好 → 怀疑推理侧问题,委托全面诊断。
> 本文记录完整证据链、三层根因、已做修复与建议。同日另修复了
> 头灯域差距(根因①),详见持久记忆 headlight-domain-gap。

---

## 一、结论速览

| 层 | 问题 | 状态 |
|---|---|---|
| ① | **渲染域差距**:数采时 teleop 把 `model.vis.headlight` 调亮(ambient .4/diffuse .8/specular .6,`MujocoTeleopController._robot_setup`),数采视频 mean≈116.6;rollout 用 xml 默认头灯 ≈82.5,差 40% | ✅ 已修(rollout 加载模型后复刻) |
| ② | **初始夹爪**:数采每回合 f0 夹爪全开 [1,1],rollout 用 home ctrl 沉降后闭合 0.21 | ✅ 已修(沉降前 ctrl=1) |
| ③ | **模型本身**:动作"发起段"与"终段逼近/闭爪时机"没学会 —— 40k 步仅 0.35 epoch,发起帧平均被采样 0.35 次 | ❌ 需训练侧解决(见四) |

①②修复后评测从"随机悬停 0.18-0.25m"进步到"稳定逼近 0.08-0.16m",但仍 0/3。
③是主瓶颈,部署侧旋钮(重推间隔 10/50、偏置补偿、动作增益、肘下压踢腿)全部无效。

## 二、根因①:头灯域差距(证据链)

- 数据集视频(28 个文件全扫)mean 恒 116.3-116.6;v3 也亮(99.7-114.8 随头位波动)。
- 排除:xml 未改(diff 只动 home 头 pitch)、record_frame 零后处理(逐字审)、
  编码链忠实(82.5 帧 → libsvtav1 → 解码 81.5)、mujoco 3.8/3.11 × egl/glfw ×
  有无 viewer 全部 82.9。
- 锁定:`XRoboToolkit-Teleop-Sample-Python/xrobotoolkit_teleop/simulation/
  mujoco_teleop_controller.py:64-66` 运行时改 headlight。复现:同状态同 xml 只改
  三行 → 82.49 → **116.98**,与数据集吻合;逐像素中位差 1、corr 0.965。
- 启示:**共享库的运行时 model 属性修改是隐形域差距来源,xml 文件永远看不出来**;
  grep `self.mj_model` 整体赋值抓不到,要 grep 属性级。

## 三、根因③:发起段与终段(证据链)

教师强制(每帧 reset 后取 chunk 第 1 步)总体 MAE 0.082,但分相拆开:

1. **发起段瘫**:f0 处 5 回合,真值右肘 50 帧内 Δ−0.43~−0.66,模型整块预测
   Δ+0.04~−0.21(有的方向都不对)。左臂发起帧(f≈766,右臂完工后)同样:
   真值 Δ−0.52~−0.73 vs 预测 Δ+0.12~−0.08。闭环在 home 死锁,缓慢漂移进
   关节空间角落(肘 1.78 > 演示 max 1.75)。
2. **中段续走学得好**:f200 处真值 0.91→0.60,预测 0.93→0.44,跟得上甚至过冲。
3. **warm-start 决定性实验**(仿真摆到演示 f150 真实状态再闭环):模型把肘
   一直压到 0(过冲,演示抓取时 0.23),停在离方块 **7cm** 处,**半空提前闭爪**
   (GripR 0.97→0.00),再张开,卡死。重推间隔 10 与 50 逐帧一致 → 模型固有行为。
   逐维偏置(全数据集均匀采样):肘 L−0.27/R−0.18、双爪 −0.12(提前闭)。
4. **采样量**:40k 步 × batch4 = 16 万样本 / 46 万帧 = 每帧 0.35 次访问;
   发起段每回合仅 2 个瞬间(右 f0、左 f766)。
5. 佐证:数据协议本身是**串行双臂**(79/79 回合右臂先动,左臂中位 f766 才动,
   依据:裁剪副本 parquet 逐回合起步帧分析)——rollout 左臂不动是复现了数据时序,
   不是故障。

## 四、建议(按性价比排序)

- **A. 续训 + 相位加权采样**(推荐):`multi_vla_dataset.py:221` 现在是均匀
  `np.random.randint` 抽帧;把发起窗口(回合 f0-60 与左臂 f700-820)和终段/抓取
  窗口(右 f250-400、左 f950-1100 附近,按每回合实际起步帧定位)的采样权重 ×5,
  再续训 40k-80k 步。不改数据、不加算力,直接补最稀缺的监督。
- **B. 纯续训**:80k-120k 步(1-2.5 天/档),每帧 0.7-1 次访问,仍不足 1 epoch,
  见效概率低于 A。
- **C. v5 数采协议**:双臂**同时**发起(消掉一半"等待"相位)、终段再放慢、
  200+ 回合。成本最高但根治。
- 训练一律由用户亲自启动(惯例);A 的 loader 改动可先准备好等确认。

## 五、当日改动文件

| 文件 | 改动 |
|---|---|
| `mjq_lingbotvla_v2/Lingbot_H1_Rollout.py` | ①加载模型后复刻 headlight 三行 ②reset_episode 沉降前夹爪 ctrl=1(均有注释) |
| 诊断脚本(临时,/tmp) | test_headlight.py, v4_bias_check.py, v4_f0_chunk_check.py, v4_warmstart.py |
| `assets/norm_stats/h1_v4.json` 等 | 未动;模型/数据/训练代码均未动 |

---

## 六、方案 A 落地(2026-08-29,用户选定 A,等用户亲自启动)

### 设计:索引展开式加权采样(不动采样器)

真采样器是 `build_dataloader` 里的 `StatefulDistributedSampler(shuffle=True)`
(`multi_vla_dataset.py:221` 的 randint 只是 __getitem__ 重试路径,不是采样)。
为了不碰 stateful/分布式/断点续训机制,采用**索引展开**:目标帧在采样空间
复制 W 份,`__len__` 变为展开长度,均匀 shuffle 自然变成加权 shuffle。

### 改动清单(除 yaml 外全部默认关闭=逐比特等价)

| 文件 | 改动 |
|---|---|
| `mjq_lingbotvla_v2/make_phase_windows.py`(新) | 从 parquet 逐回合定位 4 事件(右/左发起、右/左抓取),以锚开窗,输出绝对行号 json。已跑:302/302 全命中,锚点中位 f26/f777/f318/f1083(与诊断吻合) |
| `H1_build/data/h1_build_pick_v4_trimmed.phase_windows.json`(新) | 窗口文件:发起窗[锚−15,+75]×8、抓取窗[锚−100,+80]×4,重叠取最大 |
| `lingbotvla/utils/arguments.py` | DataArguments.`phase_window_file`(空=均匀采样);TrainingArguments.`resume_dataloader_state`(默认 true=原行为;**不补声明则 load_checkpoint_path 场景在 train_lingbotvla.py:716 直接 AttributeError→被 except 吞→静默从头训!**) |
| `lingbotvla/data/dataset.py` | `PhaseWeightedDataset`(索引展开包装)+ `build_vla_dataset` 出口挂钩;行数不符会 raise |
| `tasks/vla/train_lingbotvla.py` | 加载失败兜底收紧:显式指定 `load_checkpoint_path` 却加载失败 → RuntimeError 拒训(原行为"静默从头训"对续训是灾难) |
| `configs/vla/real_robot/h1_v4_w.yaml`(新) | 6 处差异见文件头注释;output/h1_v4_w;load 自 h1_v4/global_step_40000;resume_dataloader_state=false;max_steps 88000(续 48000 步≈27h);save_steps 8000(6 档≈164G,余 350G) |
| `mjq_lingbotvla_v2/Lingbot_H1_Training.py` | 配置区切 h1_v4_w(端口 29613;v4 回退注释保留) |
| `mjq_lingbotvla_v2/verify_phase_sampling.py`(新) | 干跑验证,5/5 全过 |

### 数字账(48k 步 × batch4 = 19.2 万样本)

| 相位 | 帧数 | 权重 | 采样占比 | 期望访问/帧 | v4 基线 |
|---|---|---|---|---|---|
| 发起(右+左) | 54 908(11.9%) | ×8 | 37.4% | **1.31** | 0.35 |
| 终段/抓取(右+左) | 109 314(23.7%) | ×4 | 37.4% | **0.65** | 0.35 |
| 中段 | 296 277(64.3%) | ×1 | 25.1% | 0.16(已学得好,让位) | 0.35 |

展开后 460 499 → 1 172 797 行(2.55×);train_steps 293 199,start_step=40 000。

### 干跑验证(verify_phase_sampling.py,未启动训练)

① yaml 走训练同入口 parse_args 全解析(新字段都已声明,无 remaining_args 坑)
② 加权构建 1 172 797 行 ✓ ③ 清空字段 → 裸 VLADataset 460 499 行(逐比特一致)✓
④ 20 万均匀抽样过映射:37.4/37.4/25.1 vs 理论 37.5/37.3/25.3 ✓
⑤ sample_map 单调 + 发起窗行展开 8 次 + 桩数据集过真 DataLoader 出批 ✓
(VLADataset.__getitem__ 本身未动、未测——免模型路径在库自身 assert 就断,
与本次改动无关;v4 已用完整路径训成 40k 步。)

### 启动(★ 用户亲自按)与中断重启

```
conda activate lingbotv2
python mjq_lingbotvla_v2/Lingbot_H1_Training.py     # VSCode ▶ 也行
```
- **中断重启前必改**:注释掉 h1_v4_w.yaml 里 `load_checkpoint_path` 两行
  (enable_resume 会自动找 output/h1_v4_w/checkpoints 最新档;不改会从 40k 重续)。
- 首个 checkpoint 48000 步(≈4.5h@2.0s/步)即可开环/闭环抽验发起段是否改善。

---

## 七、v4_w 结果(2026-08-31 训完 88000,闭环+教师强制评测)

**训练**:40000→88000 实际 46.9h(3.52s/步,比预估慢),7/7 档保存成功,无中断。

**闭环 0/10**(同 v4 协议),但有三处 v4 没有的信号:
- 右臂最小距离双峰:6/10 回合 6-12cm(v4 3 回合 8-16cm),4/10 仍在 23cm 死锁;
- 1 回合方块被碰动 5cm(v4「从未被碰」);
- 左臂恒 0.23m = 串行协议正确表现(右臂没完成,不动是对的)。

**教师强制 50 帧块级**(锚点取自 phase_windows.json,与 v4 同法;v4 基线见三章):

| 相位 | 真值 Δ50 | v4_w 预测 | v4 预测 | 判读 |
|---|---|---|---|---|
| 右发起 R_Elb@f0 | −0.31 | **−0.036** | +0.04~−0.21(方向乱) | 方向稳了,幅度仅 12% |
| 左发起 L_Elb@f777 | −0.36 | **−0.188(52%)** | +0.12~−0.08 | **明显学会** |
| 终段 R_Elb@抓前60帧 | +0.03 | **−0.463** | 过冲压限 | 大幅乱动 |
| 终段 GR@抓前60帧 | −0.42(渐闭) | **−1.018(全闭)** | 半空提前闭爪 | **过冲成满闭** |

**结论**:
1. 发起段对采样加权**有响应**(左臂 52% 幅度+方向对;右臂方向统一)——暴露量假设被证实,但右臂 f0 仍欠一个量级;
2. 终段变了失败模式:从「提前半闭」变成「满闭+肘乱动」——×4 抓取窗放大了"接近=闭爪"的错误关联,**闭爪时机的分辨(悬停 vs 触碰)大概率是视觉歧义问题,损失加权已饱和**;
3. 评测协议注意:无头默认 900 帧=30s,而演示左爪闭合在 f1083≈36s——左块在此协议下结构性无法成功,右块足够。

**下一步选项**:
- **A2(1-2 天 GPU)**:调权重再续:右发起窗前移覆盖 f0-10(onset−15 割掉了它们)、右发起 ×12、抓取 ×2 降压,88000→~120000 步。预期能把发起做到可靠,终段大概率仍弱。
- **v5 数采(根治)**:双臂并行(消灭等待相位+发起事件翻倍)、终段再放慢(多给闭爪时机样本)、200+ 回合。v4_w 的证据(暴露→发起改善;加权→终段反而过冲)指向**数据是终段的瓶颈**,不是采样。
- 两者不冲突:A2 烧卡的同时去数采。

---

## 八、FK 实测修正结论 + A2(v2 窗口)落地(2026-08-31)

### 用户纠正在先

七章"终段=视觉歧义、数据是瓶颈"的判断**说过头了**。用户指出:
数采时**夹爪闭合前后均有停顿,操作也放慢了**——数据里"停住-起闭"样本是存在的,
不是没采到。于是改用 FK 实测检验"模型悬停点 vs 操作员停顿点"差多远。

### FK 实测(20 维 state → qpos → mj_forward → 爪心坐标 vs 方块中心)

- **操作员停顿点**:爪心距方块中心 **5.0cm**(回合最小值中位 4.7cm)——
  即数据里的闭爪样本就在"贴脸"处;
- **模型悬停点**:6.1~8.8cm(v4_w 闭环 6 回合);
- **差距仅 1.5~4cm** ≈ 256px 图上 **10~20px** —— 完全可分辨,**不是不可约视觉歧义**。

终段失败的准确表述:不是"看不出该不该闭",而是**最后 1.5~4cm 的逼近平移+
停顿+起闭没学到位**——恰好是 v1 抓取窗喂偏的那段(均匀 ×4 盖一大片,大多数帧
的未来都含闭爪 → 教成"接近=满闭")。采样加权对它**没饱和,只是被 v1 窗口稀释了**。

### A2 = v2 窗口方案(已全部落地,等用户按启动键)

| 窗 | v1(已证有缺陷) | v2 | 32k 步访问/帧 |
|---|---|---|---|
| 右发起 | [锚−15,+75]×8(切掉 f0-10) | **[锚−30,+75]×12** | 1.09 |
| 左发起 | [锚−15,+75]×8 | [锚−30,+75]×8 | 0.73 |
| 右抓取 | [锚−100,+80]×4(180 帧一大片) | **[锚−60,+30]×8**(只留最后逼近+停顿+起闭) | 0.73 |
| 左抓取 | [锚−100,+80]×4 | [锚−60,+30]×8 | 0.73 |
| 中段 | ×1 | ×1(已学得好,让位) | 0.09 |

展开 460 499 → 1 403 533 行(3.05×);采样占比 w=12:26.0% / w=8:49.6% / w=1:24.4%。

### 评测协议同步修正

无头闭环评测加 `--max-steps 1800`(60s):演示左爪闭合在 f1083≈36s,
默认 900 帧=30s 把左块结构性判死(七章发现的坑)。

### 改动清单(训练代码零改动——v1 已就位的机制直接换窗口文件)

| 文件 | 改动 |
|---|---|
| `mjq_lingbotvla_v2/make_phase_windows.py` | SCHEME 字典化(v2 窗/权重),重生成 json |
| `H1_build/data/h1_build_pick_v4_trimmed.phase_windows.v2.json`(新) | 302/302 全命中,锚点中位 f26/f777/f318.5/f1083.5(与 v1 一致,检测逻辑未动) |
| `configs/vla/real_robot/h1_v4_w2.yaml`(新) | 6 处差异:窗口文件→v2 / load 自 h1_v4_w/global_step_88000 / resume_dataloader_state=false / max_steps 120000(续 32000 步≈31h@3.52s)/ output/h1_v4_w2 / save_steps 8000(4 档≈110G,余 190G) |
| `mjq_lingbotvla_v2/Lingbot_H1_Training.py` | 配置区切 v2(端口 29614;v4_w 回退注释保留) |
| `mjq_lingbotvla_v2/verify_phase_sampling.py` | 改指 v2,5/5 全过(yaml 全解析/加权 1 403 533 行/默认关闭仍逐比特一致/直方图 26.0/49.6/24.4 vs 理论/sample_map 单调+第 0 行展开 12 次) |

### 启动(★ 用户亲自按)

```
conda activate lingbotv2
python mjq_lingbotvla_v2/Lingbot_H1_Training.py     # VSCode ▶ 也行
```
- **首个新档 96000 步(≈8h)落盘后**:注释掉 h1_v4_w2.yaml 里
  `load_checkpoint_path` 两行(文件头已写),enable_resume 自动接最新档。
- 首档即可抽验:教师强制右发起 f0 块(真值 ΔR_Elb≈−0.31,v4_w 仅 12%)
  和右终段 GR(真值渐闭 −0.42,v4_w 满闭 −1.02)——这两个数是 v2 有没有
  喂对的直接读数。

## 九、推理链路审计:执行器无罪(2026-09-02,按用户上传的《排查方案》逐项执行)

背景:120000 训完闭环仍"一抽一抽",用户上传两份外部排查文档,要求按其方法
排查**我们的推理是否有问题**。方法=把"模型输出"与"动作执行器"解耦逐项查。

### ① 决定性测试:norm+映射 往返恒等(执行器侧全部通过)

拿数据集真值 (state, action×50帧) 走**训练正向** `FeatureTransform.apply` 得归一化
55 维块,假装模型完美输出它,再走 **deploy 还原** `unapply`(deploy/lingbot_vla_v2_policy.py
`_unapply_batched_actions` 同一函数)→ 应逐位复原数据集动作:

```
还原形状 (50,18)(腰不映射,rollout 补 home 腰成 20 维 —— 防御分支即正常路径)
L臂 max|还原−原始|=1.3e-08  R臂 1.4e-08  头 9e-15  夹爪 4e-09   → 恒等(float 精度)
state 训练口径 vs eval口径 max|Δ|=0.000e+00(完全相同)
55 维槽位:信号只落 L臂[0:7]/R臂[7:14]/夹爪[28:30]/头[34:36](头恒 0 正确:
  v4 头全程固定=自身 mean,且 loss 权重 ×0.0);end/腰/base/hand/尾全零
norm 文件:训练 yaml 与 rollout 默认同为 assets/norm_stats/h1_v4.json
夹爪量纲:action/state 都在 [0,1](actuator_length gear 归一化口径)
```

**norm/映射/槽位/单位/关节序,执行器侧全部一致** —— 《排查方案》§十六
normalization + action mapping 两节可以划掉。ctrl 口径也对称:数采录的
action 就是 `data.ctrl` 命令(recorder record_frame 314-318 行),rollout
把模型输出写回同一批 ctrl(多一步 ctrlrange 裁剪,命令在范围内=空操作);
state 两侧同构(qpos+actuator_length),rollout 直接 import recorder 的
UPPER_BODY_JOINTS,顺序同源。

### ② 取证回合(v4_w2@120000,无头 1200 帧,无 lead-clip 原始行为)

| 检查(方案条目) | 实测 | 裁决 |
|---|---|---|
| 块内平滑(§六) | 臂维逐帧跳变中位 0.029,p90 0.055 | ✓ 模型块内输出平滑 |
| 块边界(§七) | 边界中位 0.277,p90 0.487,max 0.712 = **9.6×** 块内 | ✗ 跳变在,且在新块第 0 步 |
| action[0]−state(§八) | 前段(f<300) 中位 0.163 → 中段 0.343 → 后段(f≥700) 0.491,max 0.851 | ✗ 超前随回合单调增大 |
| 命令−状态曲线(§十三) | \|a−s\| 臂维 max 中位 0.239(数采演示只有 0.0237,10×) | ✗ 慢性滞后回路 |
| 夹爪时序 | L 命令首闭 f800→状态 f809(9 帧伺服滞后,正常);R 全程未闭 | 伺服本身正常 |
| 推理耗时(§十五) | 含前后处理中位 421ms/次,每 1667ms 周期阻塞一次 → **冻结占空比 27%** | ✗ 画面冻结源,但不影响 sim 时间内轨迹正确性 |

CSV 证据:/tmp/debug_actions/(action_state.csv + chunk_000..023.csv,方案§五格式)。

### ③ 结论:跳变在模型输出里,不在执行器

方案§十八诊断树走到"Chunk 边界不平滑"时标的是"执行/融合问题"——**对我们
不成立**:往返恒等证明执行器如实执行;边界跳变来自模型重规划本身(chunk[0]
不锚定当前状态,超前量随回合进展 0.16→0.49 增大 = 协变量偏移累积)。执行器
无罪释放。运行时缓解(--lead-clip 0.05,已验证加速度 5.4×↓)与 421ms→
flex_cached+use_compile 提速、异步推理(消 27% 冻结)都值得做,但都治标;
拦路虎仍是终段任务行为(训练侧)。

另:外部文档"启用 Flash Attention 2 替换 Eager"路径不存在——"Using Eager
Attn"来自动作专家 `get_attention_interface()`,只有 flex/flex_cached/eager
三选一;且 eager 是 deploy/lingbot_vla_v2_policy.py:319 硬编码覆盖(训练存
的是 flex_cached)。提速的正道=改回 flex_cached + use_compile,需实测。

## 十、开局行为验证:无"放桶意图",= 发起幅值欠额 + 边界超前累积漂移(2026-09-02)

用户观察:推理开局右臂先往左上方移动"像在放东西",之后沿数采大致轨迹一抽一
抽,不夹取,左臂几乎不动。数采协议:右臂先动(水平右移→下移→前伸抓取),
左臂前 200 帧完全静止。

### 探针与结论(修正后)

⚠️ 方法论坑(先自纠):第一版 home 探针有两个 bug——① chunk 是 18 维
[L7,R7,头2,爪2] **不含腰**(rollout 源码 `concat([home_ctrl[:2], action])`
证实),直接写 qpos[QADR] 会 L7→腰槽、R7→左臂槽(FK 镜像错位);② lead 比
较用了被自己 fk 污染的 env.get_state()。由此报出的"左臂 1m 甩动 / chunk[0]
超前 2 rad"**全部作废**。正确做法:FK 前补 home 腰;超前一律对**快照 state**。
修正版含防改动检查(观测数组 5 次推理后 |Δ|=0)。

| 探针 | 实测 | 解读 |
|---|---|---|
| home 同观测重复采样 5+1 次 | 右臂第一块位移 1-6cm、左臂 1-3cm,方向样本间不一致 | **无系统性"左上放桶"意图**;开局规划≈静止小晃 |
| 第一块幅值 vs 演示发起段 | 演示 75 帧下手 ~10cm+;模型 50 帧仅 1-6cm(≈11-20%,与教师强制 f0 11% 一致) | 发起程序幅值严重欠额 |
| chunk[0]−state 超前(对快照) | 左肘 L_J6 **固定 ~0.17 rad 每次都在**;右臂 0.04-0.17 rad;演示动作−状态中位 0.0014-0.024 | 输出噪声地板比演示高 10-100×;固定偏置在左肘 |
| 执行 f0→f150(取证回合) | 右指端累计上漂 9cm、向中线 y −0.21→−0.07(右块在 y=−0.30,距离反增 0.22→0.35m) | 单块规划仅 3.8cm → **漂移是 replan 边界超前(0.16 rad 级)逐块累积**,方向偏向上/中线 = 演示里右臂高频停留区(运送/停放),非显式规划 |
| 爪 | home 命令 0.94-0.96(演示 1.0 全开) | 轻微闭爪偏置;臂到不了方块,闭爪时机永不触发 |
| 左臂 | 模型开局对左臂也只规划 1-3cm 小晃 | **"开局不动左臂"其实学对了**,静观感来自噪声地板的微漂 |

### 对下一步的含义

- 执行器仍无罪;运行时增益补偿**降级为不做**(会等比放大噪声与欠幅,方向还不稳)。
- 三层机制定案:①发起程序幅值 ~11%(学过但欠额);②输出噪声地板 10-100× 演示
  → 每个 replan 边界命令跳 0.04-0.18 rad = "一抽一抽";③边界超前方向偏置向上/
  中线 → 用户看到的"先左上像放东西"。全部指向训练侧曝光不足(120k 步 ≈ 1.04
  epoch),续训 120k→200k 优先级最高。
- 廉价体检门限(每次续训后 2 分钟):修正版探针 /tmp/home_chunk_probe2.py 看
  (a) 右臂第一块幅值是否涨向演示量级 (b) L_J6 0.17 rad 固定超前是否收敛
  (c) 方向是否稳定指向右块(下移+右移)。

## 十一、w3 终审(2026-09-04):专家-only 配方封顶,定案

**实验设计**:w3 = w2 逐字配方续训(120k→152k,152k 为夜间无人值守临时 max_steps 自动收工),
逐档 home 探针看门指标斜率。判据(2026-09-03 预先约定):幅值回落/原地 + L_J6 仍 0.18 → 封顶。

**全系列数据**(home 同观测 5 次采样;门:右首块幅值≥10cm、L_J6 超前<0.05):

| 档 | 幅值均值 | 最好样本 | z 方向(演示=向下) | L_J6 超前 |
|---|---|---|---|---|
| 120k(基线) | ~3-4cm | ~6cm | 乱(有向上) | 0.17 |
| 128k | 4.9cm | 7.2cm | 2/5 下,1 上 | 0.18 |
| 136k | 3.2cm | 5.0cm | 3/5 下,1 上 | 0.19 |
| 144k | 5.9cm | 9.1cm | 5/5 下(2 个达演示 −0.08) | 0.18 |
| 152k | 3.9cm | 5.7cm | 3/5 下(再现 +0.04 向上) | 0.18 |

**判读**:
① 幅值 4.9→3.2→5.9→3.9cm 在 4-6cm 带内振荡,无斜率(门 10cm,停在门的 40-60%);
② 144k 的"系列最佳"(5/5 下移+两个演示量级样本)在 152k 全部回吐 → 是采样噪声不是趋势;
③ L_J6 超前五个档钉死 0.17-0.19:32000 步 ×(发起×12 加权)零修正,而演示数据里该超前
   不存在(|a−s| 中位 0.0013)→ 纯模型侧系统输出偏置,专家-only 修不动。

**结论**:"曝光不足"假设证伪。152k 步 = 60.8 万样本 = 外部成功用量的 15 倍,幅值仍在门下半段
振荡。专家-only 配方已收敛到它能到的位置,剩余 48k 步(≈2 天)按此系列只会继续振荡,不烧。

**含义**:唯一活口假设 = 冻 VLM 特征适配不了本域(配方封顶)。下一实验 = 全模型配方:
租 4×A100 时多卡 fsdp_enabled=True,官方配置原样可跑,无需 offload/无需动框架。
版本清单已交付用户:python 3.10/3.12 + torch 2.8.0+cu128 + flash-attn 2.8.3(官方 requirements.txt)。

**产物与工具**:w3 四档(128/136/144/152,各 27.4G);home_chunk_probe2.py 已参数化
(可传任意 hf_ckpt 路径);w3 yaml max_steps 现为 152000(若续训需改回)。
