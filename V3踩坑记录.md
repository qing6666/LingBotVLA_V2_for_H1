# V3 踩坑记录 —— 闭环 0% 抓取成功率排查全复盘

> **对象**：checkpoint `output/h1_v3/checkpoints/global_step_20000/hf_ckpt`
> （norm: `assets/norm_stats/h1_v3.json`）｜数据集 `h1_build_pick_v3_trimmed`（148 条）
> ｜闭环现象：双臂抓取/放置全部偏"斜上方"，完全成功 0/10
> **排查时间**：2026-08-21 ~ 08-25
>
> **结论先行**——三层问题叠加，只有第三层是根因：
> 1. 帧 0 亮度差：**自愈，不是问题**，不用修；
> 2. 右臂常值关节偏置：**可补偿**（`--bias-comp v3right`），但补偿后仍失败；
> 3. 终末缩水平衡：**根因**。flow-matching 对"又快又风格各异"的终末段做条件平均，
>    模型把一切终末动作预测成缩水版，闭环永远差最后 6~8cm——只能靠 v4 数据协议解决。

---

## 坑 1：帧 0 亮度差 ≠ 渲染域差距

- **现象**：rollout 开始瞬间画面比数采暗/亮，怀疑仿真渲染域差距。
- **实际**：是**帧 0 场景内容不同**（方块位置、静置状态与数据首帧不一致）造成的，
  数秒内自愈，对成功率无影响。
- **教训**：看到"域差距"先分类——**自愈的不用修**。修它纯属浪费一周。

## 坑 2：右臂抓偏左上 —— 不是头部相机的锅

- **现象**：每次都抓在方块斜上方、放在桶斜上方；右臂偏左上、左臂偏右上（对称）。
  一度怀疑是"数采时头部俯仰旋转"导致训练视角漂移。
- **实际**：开环诊断（预测动作 vs 数据真值按 k 步对齐）发现右臂三个维度有
  **系统性常值偏置**，与头部无关：

  | 维度 | 含义 | MAE | 偏置 |
  |---|---|---|---|
  | dim 9 (R_J1) | 右肩 | 0.2253 | **+0.1908 rad** |
  | dim 12 (R_J4) | 右肘 | 0.2347 | **−0.1631 rad** |
  | dim 19 (Grip.R) | 右夹爪 | 0.2673 | **−0.2601 rad** |

  偏置集中在 chunk 起点（k=0 最大，k=49 趋零）→ 是预测问题不是执行问题。
  补偿后右臂抓取高度确实下来了——**证明偏置真实存在，但它不是全部**。
- **教训**：
  1. "感觉抓偏"先量化再归因——rollout 的 EE-方块最小距离+偏移向量日志
     （`偏移[前0.05 右0.01 上0.18]`）就是为此加的；
  2. 用**开环 k 步对齐**把"常值偏置"和"整段缩水"分开，两者修法完全不同；
  3. 20 维布局备忘：`[0:2]`腰 `[2:9]`左臂 `[9:16]`右臂 `[16:18]`头 `[18:20]`夹爪。

## 坑 3：终末缩水平衡 —— 根因，增益救不了

- **现象链**（同一 checkpoint 逐级加码）：
  - 裸跑：抓/放都偏上；
  - `--bias-comp v3right`：高度降低，仍偏；
  - `--action-gain 1.3`（单独）：仍偏上（增益把偏置也放大了 1.3 倍）；
  - `--bias-comp v3right --action-gain 1.3`：右臂抓取已在方块近正上方，
    放置仍偏——剩余残差是**纯竖直方向 ~0.06m**；
  - `--action-gain 1.5`：下压到了方块高度，但路径变形，伸臂时侧面把方块推歪，左侧抓空。
- **根因**：v3 数据终末段**又快、又风格各异**（进近方向/斜插/横扫都有，最后 5cm 一晃而过）。
  flow-matching 专家学的是条件分布的**平均**——多风格终末段的平均 = 一段"缩水的动作"，
  闭环执行缩水动作 → 永远停在目标前 6~8cm（沿进近方向）。两臂对称偏"内上方"正是
  "平均后的进近"的几何投影。
- **教训**：
  1. `--action-gain` 是**诊断工具不是修复工具**：它把"欠程"换成"路径变形"；
  2. 增益实验必须与 `--bias-comp` 配对，否则增益在放大缩水的同时也放大偏置，结论会混；
  3. 对 flow-matching VLA：**终末段风格一致性 >> 位置多样性**。
     这就是 v4 协议"正上方进近 + 最后 5cm 慢速 + 放置同理"的直接依据。

## 坑 4：头部相机的三个想当然

1. **"固定俯视能让方块像素变大"——错**。像素大小由**距离**决定
   （头部相机 ~1.3m 高 vs 桌面 0.641m，距离下限 ~0.8m），俯仰只影响居中。
   想让方块变大只能把方块挪近（v4 正是这么做的）。
2. **"推理时头不动、数采时头能动，必须对齐"——部分错**。头部本来就是模型控制的
   （dims 16/17），推理时会自己纠到 ~0.49。实测 v3 数据：首帧 yaw 散射仅
   std 0.019 rad（±1°），pitch std 0.058 rad；回合内 yaw 平均摆 0.149 rad。
   **不是 0% 的主因，但锁死是免费收益**——v4 干脆把头部完全固定（见坑 6）。
3. **"头显低头看不到双臂是 bug"——正常**。第一人称几何：头俯得越低，
   视野越是桌面近景，手臂自然出画。方块挪到正中后正常俯角即可同框。

## 坑 5：随机出生带 —— 想法美好，实测翻车

- **想法**：位置随机给泛化，风格统一给精度。
- **实测**：遥操作对随机落点舒适度不一，**很多位置根本不好抓**，
  操作员硬凑的动作只会给数据引入新的风格噪声。
- **教训**：**固定点还 0% 成功时，随机化只是加难度，不是加泛化**。
  先把固定点做到高成功率，再谈随机化。随机带基础设施保留在
  `H1_simulation_model/H1_build/teleop/h1_spawn_zones.py`（标记【备用】），v5 想启用随时能开。

## 坑 6：口径漂移 —— 用 keyframe 做单一事实来源

- v3 期间最大的隐性坑：**出生点/初始姿态散落在多处**（XML keyframe、评测脚本、
  操作员肌肉记忆），一旦某处改了别处没跟，训练/评测口径就悄悄分叉。
- v4 落实：**一切初始状态只写 `H1_scene.xml` 的 home keyframe 一份**——
  方块 (0.55, ±0.12)、头部 (0, 0.53)。数采 A 键重置、B 键回 home、
  rollout `--spawn home`、离线校验全部读它。
- 顺带修掉的机制坑：B 键 homing **只回上半身关节、不动方块**；
  keyframe 复位逻辑只在 `--check-model` 离线检查里，别再找错地方。

---

## 由此定下的 v4 协议（对策汇总）

| 项 | v3（坑） | v4（对策） |
|---|---|---|
| 出生点 | (0.65, ±0.30) | **沿用不变**（曾改双臂之间 (0.55,±0.12)、试过随机带，均按用户要求改回/弃用） |
| 头部 | 跟头显 yaw+pitch，首帧/回合内都有散射 | **完全固定** (0, 0.53)，overlooking 相机，dims16/17 为常量 |
| 进近 | 斜插/横扫都有 | **统一正上方进近**，禁止横扫 |
| 终末 | 最后 5cm 一晃而过，风格各异 | **最后 5cm ≈1s**，闭爪停 0.5s，放置同理 |
| 条数 | 148 | 目标 **300** |
| 训练侧 | 无差别损失 | **per-dim 加权**（右臂 7 维+双夹爪，对准实测偏置维度） |
| 流程 | — | QC → trim **拷贝**（不动原数据）→ **重新生成 norm_stats** → 训练配置路径同步改 v4 |

## 诊断命令备查

```bash
# 复现诊断链（按当年顺序）
python mjq_lingbotvla_v2/Lingbot_H1_Rollout.py                                  # 裸跑看现象
python mjq_lingbotvla_v2/Lingbot_H1_Rollout.py --bias-comp v3right              # 剥离右臂常值偏置
python mjq_lingbotvla_v2/Lingbot_H1_Rollout.py --bias-comp v3right --action-gain 1.3   # 暴露纯缩水残差
python mjq_lingbotvla_v2/Lingbot_H1_Rollout.py --bias-comp v3right --action-gain 1.5   # 缩水确认(会推歪方块,仅诊断)

# v4 评测口径
python mjq_lingbotvla_v2/Lingbot_H1_Rollout.py --headless                       # --spawn home=v4固定点(默认)
python mjq_lingbotvla_v2/Lingbot_H1_Rollout.py --headless --spawn v3            # 老点,新老对比
```

> 注：`--bias-comp`/`--action-gain` 只用于**诊断 v3**。v4 模型评测一律裸跑——
> 需要补偿才能工作的模型不算修好。
