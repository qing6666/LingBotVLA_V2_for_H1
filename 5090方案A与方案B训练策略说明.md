# 5090 上方案 A 与方案 B 训练策略说明

> 日期:2026-09-08 | 状态:方案 B 续训 40000 步进行中(2026-09-08 启动,自 20000 档接续)
> 配套文件:`configs/vla/real_robot/h1_v4_w3.yaml`(A 终版)、`configs/vla/real_robot/h1_v4_full.yaml`(B)、
> 《B方案5090训练说明.md》(B 的补丁/原理细节)、《V4闭环失败诊断.md》(A 系失败分析)

---

## 一、总览:一句话说清两条路线

| | 方案 A(w / w2 / w3 系) | 方案 B(h1_v4_full) |
|---|---|---|
| 一句话 | **只训 1.7B 动作专家**,冻结 Qwen3-VL 4B 主干 | **解冻整个 6.2B 模型**一起微调 |
| 核心开关 | `train_expert_only: true` | `train_expert_only: false` |
| 显存策略 | 直接装进 32G(无 offload) | FSDP2 CPU offload(参数驻内存,按步上卡) |
| 单步耗时 | 3.52 s | 15.09 s |
| 结局 | 152k 步封顶,闭环 0% | 20k 步首次双臂发起+趋近,续训中 |

两条路线**数据、加权窗口、归一化、动作损失权重、chunk、损失函数、优化器逐字相同**,
唯一实验变量是"训不训 VLM"——因此结果可以直接归因。

---

## 二、共同基础(两案都不变的东西)

| 项 | 取值 | 说明 |
|---|---|---|
| 模型 | LingBot-VLA 2.0 6B(Qwen3-VL-4B 主干 + 1.7B MoE 动作专家,597.6M 激活) | 官方底座 `models/lingbot-vla-v2-6b` 起训 |
| 数据 | `h1_build_pick_v4_trimmed`(300 条遥操作轨迹) | 同一份 |
| 加权采样 | `phase_windows.v2.json`(右发 ×12 / 左发 ×8 / 抓取 ×8) | 同一份 |
| 归一化 | `assets/norm_stats/h1_v4.json`(头部 std 打 0.02 地板) | 同一份,绝不能换 |
| 动作空间 | 55 维统一槽位,18 维活跃(臂 14+头 2+夹爪 2);右臂与双夹爪损失 ×2.0,头部 ×0.0 | 同一份权重表 |
| 动作块 | chunk_size 50 @ 30Hz(约 1.67s 规划) | 同 |
| 损失 | L1_fm(flow matching) | 同 |
| 优化器 | muon,lr_decay_style constant | 同 |
| 精度 | 纯 bf16(enable_fp32 false,enable_mixed_precision false) | 同 |
| 硬件 | RTX 5090 32GB + 125G 内存,单卡 | 同 |

---

## 三、方案 A:专家-only(冻结 VLM)

### 3.1 策略内容

`train_expert_only: true` —— 反向传播只更新 **1.7B MoE 动作专家**;
Qwen3-VL 4B 主干(视觉塔 + 语言模型)**整体冻结**,不产生梯度、不进优化器。
推理链路上 VLM 照常前向(图像→表征→喂给专家),只是它的参数永远是官方预训练时的值。

> 注:A 配置里 `freeze_vision_encoder: false` 看似矛盾,实际无效果:
> `train_expert_only: true` 已把整个 VLM(含视觉塔)排除在训练之外,
> `freeze_vision_encoder` 只在解冻 VLM 后才起作用(方案 B 里它决定视觉塔是否单独用 vit_lr)。

### 3.2 关键训练参数(终版 w3 实测配置)

| 参数 | 值 | 备注 |
|---|---|---|
| train_expert_only | **true** | 定义性开关 |
| lr | **1.0e-4** | 专家-only 仿真配方(偏大,只有专家要训) |
| micro / 累积 / gbs | 1 / **4** / **4** | 32G 装得下,可开梯度累积 |
| 并行 | fsdp2 单卡,`enable_full_shard: false`,**无 offload** | 专家三件套(权重+梯度+动量 ≈10G)+ 冻结 VLM 前向权重 ≈12.4G + 激活,直接进 32G |
| max_steps | 152000(原计划 200000,探针判封顶后停) | |
| save_steps | 8000(每档 ≈27.4G) | |
| 单步耗时 | **3.52 s** | 无 offload 搬运开销 |

### 3.3 训练链条与资源消耗

A 不是一个跑,而是一条互相续训的链(全部专家-only 同配方):

| 批次 | 步数区间 | 实验变量 | 累计样本(×gbs4) |
|---|---|---|---|
| v4 | 0 → 40000 | 基线(均匀采样) | 16 万 |
| v4_w | 40000 → 88000 | 相位加权采样 v1 | 35.2 万 |
| v4_w2 | 88000 → 120000 | 加权 v2 窗口(右发 ×12 等) | 48 万 |
| v4_w3 | 120000 → 152000 | 同配方再压 32000 步(赌"曝光不足") | **60.8 万** |

60.8 万样本 ≈ 外部成功复现(1 万步 × gbs4,约 4-5 万样本)的 **15 倍**。

### 3.4 为什么当时只能选 A

全模型微调的可训练参数 ≈5.7B,训练态三件套(权重+梯度+muon 动量,各 ≈11.4G)
合计 **≈34.3G > 32G**,5090 单卡物理装不下;offload 路线当时还没打通
(即方案 B 的 4 个补丁)。专家-only 只需 ≈10G 训练态,是当时唯一跑得起来的选择。

### 3.5 结果与封顶判定

加权采样的功劳是真的:v4→v4_w 把左臂发起概率从 0 拉到 52%,证明"曝光→学习"因果链成立。
但 home 探针逐档门指标(目标:右首块幅值 ≥10cm / L_J6 超前 <0.05 / 方向→右块):

| 检查点 | 发起幅值 | L_J6 超前 | 方向 |
|---|---|---|---|
| 120k | 门的 40-60%(4-6cm) | 0.17 | 不一致 |
| 128k~152k(五档) | **纹丝不动** | 0.17-0.19 | 不一致 |

15 倍于外部的样本量、五档检查点零变化 → 判定**配方封顶**:瓶颈不在数据量,
在"冻结的 VLM 特征不适配本域"。闭环成功率全程 0%。

---

## 四、方案 B:全模型微调(解冻 VLM)

### 4.1 策略内容

`train_expert_only: false` —— 解冻整个 6.2B 模型,**5.7B 参数可训练**:
VLM(视觉塔+语言模型)与动作专家一起微调。视觉塔单独压小学习率防遗忘。
起点 = **官方底座冷启动**(不从 w3 热启动:w3 的专家可能已把"向均值收缩"行为练瓷实,有继承风险)。

### 4.2 关键训练参数

| 参数 | 值 | 备注 |
|---|---|---|
| train_expert_only | **false** | 定义性开关 |
| lr | **5.0e-5** | 官方真机配方(全模型要压低) |
| vit_lr | **1.0e-6** | 视觉塔独立小学习率(官方默认值),轻调适配本域、不冲掉预训练能力 |
| micro / 累积 / gbs | 1 / **1** / **1** | offload 与梯度累积互斥,batch 只能 1 |
| 并行 | fsdp2 单卡 + **enable_fsdp_offload: true** + enable_full_shard: true | 三件套驻 125G 内存,每步用到哪块搬上卡 |
| 单步耗时 | **15.09 s** | offload 搬运开销 ~4s/步 |
| 峰值显存 | 12.32 G / 32G | |
| max_steps | 40000(20000 已完成,续训中) | 官方 real_robot 默认值 |
| save_steps | 2500(每档 ≈36G) | 落在 2-4k 步探针窗口 |

### 4.3 与方案 A 的 8 处配置差异及理由

| # | 项 | A → B | 为什么 |
|---|---|---|---|
| ① | train_expert_only | true → **false** | ★核心:解冻整个 VLM,补上"视觉特征不适配"这唯一缺口 |
| ② | lr | 1e-4 → **5e-5** | 可训练参数从 1.7B 变 5.7B,按官方真机配方压低 |
| ③ | vit_lr | 无 → **1e-6** | 视觉塔解冻后必须单独给小学习率,保护预训练视觉能力 |
| ④ | model_path 起点 | w 系互相续接 → **官方底座冷启动** | 与外部成功复现同路径,最干净对照;避开 w3 收缩行为继承 |
| ⑤ | enable_fsdp_offload | false → **true** | 34.3G 训练态 > 32G,唯一装下的办法 |
| ⑥ | enable_full_shard | false → **true** | offload 需要前向后释放 unsharded 参数,否则 11.5G 权重常驻 GPU |
| ⑦ | 累积/gbs | 4/4 → **1/1** | offload 与梯度累积互斥 |
| ⑧ | save_steps | 8000 → 2500 | 全模型新配方,前 2-4k 步是探针关键窗口,要密落档 |

其余(数据/加权/归一化/动作权重/MoE 结构/损失/优化器/精度)**逐字未动**。

### 4.4 offload 与 4 个补丁(简述)

offload 把训练态放进内存后,任何集合通信(NCCL)遇到 CPU 张量就崩,且 muon 的
Newton-Schulz 正交化在 CPU 上慢 100 倍。由此打了 4 个可复原补丁
(备份/diff/复原命令:`mjq_lingbotvla_v2/B_patch/README.md`,原理详见《B方案5090训练说明.md》):

| # | 文件 | 解决什么 |
|---|---|---|
| 1 | `lingbotvla/distributed/torch_parallelize.py` | 原代码单卡直接拒绝 fsdp2+offload,放行 |
| 2 | `tasks/vla/train_lingbotvla.py` | 梯度裁剪对 CPU DTensor 崩 → 单卡走 to_local 零通信 |
| 3 | `lingbotvla/optim/muon.py`(2 处) | muon 集合通信对 CPU 张量崩 → 1 卡恒等化(数学等价,零通信) |
| 4 | `lingbotvla/optim/muon.py`(4 调用点) | Newton-Schulz CPU 上 10-20 分钟/步 → 借 GPU 算(<2s) |

### 4.5 与官方 4×A100 配方的 3 处有意偏离

官方 `real_robot.yaml` 本身就是全模型微调,B 与其核心完全一致(lr 5e-5 / vit_lr 1e-6 /
muon / constant / L1_fm / micro 1)。因单卡 offload 被迫偏离:

| 偏离 | 官方 | 我们 | 影响 |
|---|---|---|---|
| global_batch_size | 4(4 卡) | 1 | 样本效率低,靠步数补(40k 步 ≈ 官方 10k 步的样本量) |
| enable_fp32 | true | false(纯 bf16) | 速度/显存换精度,loss 曲线正常 |
| use_compile | true | false | 首跑不编译,快起稳跑 |

---

## 五、A vs B 配置并排总表

| 项 | 方案 A(w3 终版) | 方案 B(h1_v4_full) |
|---|---|---|
| train_expert_only | true | false |
| 可训练参数 | ≈1.7B(仅动作专家) | ≈5.7B(全模型) |
| lr / vit_lr | 1e-4 / — | 5e-5 / 1e-6 |
| 起点 | 官方底座(v4 起,链式续训至 152k) | 官方底座(冷启动) |
| micro/累积/gbs | 1/4/4 | 1/1/1 |
| offload / full_shard | 无 / false | 有 / true |
| 工程补丁 | 0 个 | 4 个(可复原) |
| 单步耗时 | 3.52 s | 15.09 s(4.3×) |
| 峰值显存 | —(常规占用) | 12.32 G |
| 每档体积 | 27.4 G | 36 G |
| 总步数/样本 | 152k 步 / 60.8 万样本 | 40k 步(进行中)/ 4 万样本 |
| 数据/加权/归一化/损失/优化器 | 相同 | 相同 |

---

## 六、原理对比:冻结特征 vs 适配特征

**A 的病**:动作专家学动作,靠的是 VLM 给的视觉表征——把三路相机图像+指令压缩成特征向量,
专家再从中读出"方块在哪、该往哪伸"。冻结的 VLM 是官方预训练时固化的,
从没见过我们的头灯光照、桌面布局、桶位和腕部视角,表征里"方块在哪"本身就是糊的。
专家再聪明、数据再多,等于拿着拍糊的地图找路。w3 用 15 倍样本证明了加数据救不了冻结特征。

**B 的药**:VLM 一起微调,让表征本身适配本域。视觉塔用 vit_lr=1e-6 轻调
(通用视觉能力是资产,给大了会灾难性遗忘),语言/动作/映射部分用 lr=5e-5 正常训。

类比:A 是好师傅(动作专家)配完全不懂行的翻译(冻结 VLM),师傅永远听不清指令;
B 是连翻译一起送去培训。

---

## 七、实测结果对比

### 7.1 行为指标(B@20000,仅用 A 封顶时 1/30 的样本量)

| 指标 | A(w3@152k) | B(@20000) |
|---|---|---|
| 发起 | 从未(幅值钉死 11-20% ≈ 4-6cm) | **双臂发起**,幅值带 5.9-16.1cm,破 A 的波段 |
| 趋近方块 | 无 | 双臂趋近,右指尖最近 0.142m |
| 方向 | 不一致 | **20/20 指向目标块** |
| 闭环成功率 | 0% | 0%(定位/抓/放未学会,差距在收窄) |
| L_J6 超前 | 0.17-0.19 | ≈0.18(两案都未解决) |
| chunk 内抖动 | — | 21.4→11.0 rad 单调降(仍在收敛) |
| loss | — | 1.31→0.22,20000 步处仍未饱和 |

### 7.2 成本对比

| 成本项 | A | B |
|---|---|---|
| 每步墙钟 | 3.52 s | 15.09 s |
| 每 1 万样本耗时 | 3.52×2500 ≈ 2.4 h | 15.09×10000 ≈ 42 h |
| 工程维护 | 无补丁 | 4 补丁(有备份,三条 cp 复原) |
| batch 灵活性 | 可累积 | 锁死 1 |

B 单位样本的成本约为 A 的 17 倍——但 A 的方向是错的,快也无用;
B 用 2 万样本拿到了 A 60.8 万样本从未拿到的行为。

---

## 八、结论与现状

1. **A 证明"再多数据也救不了冻结特征"**(152k 步五档钉死);**B 证明"解冻就对路"**
   (2 万样本即双臂发起+趋近)。B 的中间优势尚未兑换成端到端成功,正在续训补火候。
2. 现状:B 自 20000 档续训至 40000(≈3.5 天,样本量 4 万 ≈ 外部成功复现的 80%);
   磁盘已清(余 562G),中间档 2500-17500 与 TRIAL 已删,保留 20000 终档与 w3@152000/hf_ckpt。
3. 启动/复原/回滚细节:《B方案5090训练说明.md》第四、六章。
