# B 方案(全模型微调)在 5090 上训练说明

> 日期:2026-09-04 | 状态:20000 步已完成,续训 40000 中(见第六章)
> 配套文件:`configs/vla/real_robot/h1_v4_full.yaml`、`mjq_lingbotvla_v2/Lingbot_H1_Training_Full.py`、补丁备份 `mjq_lingbotvla_v2/B_patch/`

---

## 一、方案 B 的原理

### 1.1 为什么会有方案 B

v4 系列全部训练(w / w2 / w3)都用的 **专家-only 配方**(`train_expert_only: true`):
只训 1.7B 动作专家,Qwen3-VL 4B 主干(视觉塔+语言模型)整体冻结。

w3 终审(2026-09-04,详见《V4闭环失败诊断.md》第十一章)判了封顶:

| 检查点 | 步数 | 样本量 | 发起幅值 | L_J6 超前 |
|---|---|---|---|---|
| 120k | 12 万 | 48 万 | 门的 40-60% | 0.17 |
| 128k~152k | 12.8~15.2 万 | 51~61 万 | 纹丝不动 | 0.17-0.19 |

外部成功复现只用了 **1 万步、5 万样本**;我们喂了 15 倍的量,五档检查点毫无变化。
数据量、步数、动作权重、加权采样全部排除后,唯一活着的假设:

> **冻结的 VLM 特征不适配本域**——视觉主干从没见过我们的头灯/桶位/桌面布局,
> 动作专家拿到的输入表征本身不够好,专家再怎么练也补不上。

佐证:官方 `real_robot.yaml` 和 SO101 成功复现**都是全模型微调**
(`train_expert_only` 默认 false)。我们此前因 32G 显存装不下,被迫只训专家。

### 1.2 方案 B 定义

**解冻整个模型(6226.4M 参数)一起微调**,其余一切与 w3 逐字相同:
同一份数据、同一份加权窗口、同一份归一化统计、同一份动作损失权重。
**唯一实验变量 = 配方**。这样结果可以直接归因。

- 语言/动作部分:`lr 5.0e-5`(官方真机配方;专家-only 时代用的 1e-4 偏大)
- 视觉塔:`vit_lr 1.0e-6`(独立小学习率,只做轻微微调,别把预训练视觉能力冲掉)

### 1.3 冷启动选择

权重起点 = **官方底座 `models/lingbot-vla-v2-6b`**,全新优化器,步数从 0 计。

- 与外部成功复现(50 回合 / 4×A100 / 1 万步)路径完全一致;我们数据是其 6 倍——最干净的对照
- 不用 w3@152000 热启动:专家可能已把"向均值收缩"的行为练瓷实,有继承风险

### 1.4 5090 上的核心矛盾(为什么必须 offload)

全模型微调的可训练参数 ≈ 5.7B,训练时要同时在显存里放三样:

| 项目 | bf16 大小 |
|---|---|
| 权重 | 11.4 G |
| 梯度 | 11.4 G |
| muon 动量缓冲 | 11.4 G |
| **合计** | **≈ 34.3 G > 32 G** ✗ |

纯 GPU 放不下。唯一解法:**FSDP2 CPU offload**——三样东西平时住进 125G 内存,
每一步用到哪块才搬上显卡。代价是引入了下面 4 个补丁要解决的问题。

---

## 二、改了哪些部分、为什么(4 个补丁)

全部备份/diff/复原命令在 `mjq_lingbotvla_v2/B_patch/README.md`。总览:

| # | 文件 | 改动量 | 解决什么 |
|---|---|---|---|
| 1 | `lingbotvla/distributed/torch_parallelize.py` | 4 处 | 放行"单卡 fsdp2 + offload"(原代码单卡直接拒绝) |
| 2 | `tasks/vla/train_lingbotvla.py` | ~L965,9 行 | 梯度裁剪在 CPU 张量上崩 |
| 3 | `lingbotvla/optim/muon.py` | 2 处 | muon 优化器的集合通信在 CPU 张量上崩 |
| 4 | `lingbotvla/optim/muon.py` | 1 包装 + 4 调用点 | Newton-Schulz 在 CPU 上慢 100 倍 |

### 补丁 1:torch_parallelize.py —— 放行单卡 offload

**原逻辑**:`fsdp_enabled = fsdp_size > 1` → 单卡恒 False → 开 offload 直接 raise,
fsdp2 分支整段跳过。offload 是 FSDP2 的能力,但作者只考虑了多卡场景。

**改动**:
1. import 区加 `from torch.distributed.fsdp import CPUOffloadPolicy`
2. 函数开头预读 `enable_fsdp_offload` 标志(只 get 不 pop,fsdp1 分支仍自行 pop)
3. 守卫改为 `world_size > 1` 才 raise(多卡照旧拒绝,单卡放行)
4. 大门 `if parallel_state.fsdp_enabled or enable_fsdp_offload:` → 单卡+offload 也进 fsdp2 分支
5. 分支内把 `CPUOffloadPolicy(pin_memory=True)` 挂到 `fsdp_kwargs` / `mp_fsdp_kwargs` 的 `offload_policy`

**为什么可行**:单卡时 dp_shard mesh 本来就存在(尺寸 1),FSDP2 包装、offload 机制全部正常工作。

### 补丁 2:train_lingbotvla.py —— 单卡梯度裁剪

**死因**(试跑 1,启动 7 秒崩):offload 下梯度是 CPU 上的 DTensor,
torch 的 `clip_grad_norm_` 会经 mesh 发 NCCL 集合通信,而 NCCL 只认 GPU 张量:
`RuntimeError: No backend type associated with device type cpu`。

**改法**:新增 `world_size == 1` 分支——对 `p.grad.to_local()`(1 卡 mesh 的 local=全量)
直接裁剪,零通信。单卡无任何通信需求。

**行为不变**:普通张量没有 `to_local`,原样通过;多卡走原分支。w 系列全部历史跑法逐比特不变。

### 补丁 3:muon.py —— 集合通信单卡恒等化

**死因**(试跑 2,崩在 `optimizer.step()`,muon.py:542):同一类病——
muon 对 CPU 张量发 `dist.all_gather`,NCCL 不收。整条 muon 路径共两处集合通信:

**改法**(数学上都是"1 卡集合通信 = 恒等变换",零通信结果相同):
1. `_full_grad`:`full_tensor()` 内部隐式 all_gather → mesh 总尺寸==1 时 local 即全量,
   恒等返回。这一处覆盖 3 个调用点(含 3D MoE 专家张量那条道,108 个专家张量就在里面)
2. `_step_megabatch_chunk` 的显式 `all_gather` → `world_size == 1` 时直接 `copy_`

**行为不变**:多卡(mesh>1)走原路;w 系列参数是普通张量,根本不进 DTensor 分支。

### 补丁 4:muon.py —— Newton-Schulz 借 GPU 算

**死因**(试跑 3,不是崩,是速度死刑):不崩了,但 muon 在 CPU 上对 5.7B 参数做
Newton-Schulz 正交化——纯矩阵乘,≈2×10¹⁴ flops,24 核全速磨 **10-20 分钟/步**。
实测:第 0 步磨了 14 分钟没磨完,全程 24 核打满。20000 步 ≈ **208 天**,直接宣判不可用。

**改法**:新增 `_newton_schulz_maybe_gpu` 包装——张量在 CPU 且有 GPU 时,
搬上 5090 算完(<2 秒)再搬回。4 个 NS 调用点全部换用。

**为什么安全**:
- NS 是纯矩阵乘,无任何跨步状态,搬设备算完搬回,语义完全等价
- 内部 `compute_dtype=bfloat16`,且会先转置保证中间方阵取小边(最大 32×2560²),
  GPU 峰值显存 ~4G,余量充足
- 同算法同精度;唯一差异是浮点最低位舍入随 CPU/GPU 实现略有不同——对优化器无语义影响
- 张量本来就在 GPU(多卡/无 offload)时直通,零改动

### 三次试跑死因链(时间线)

| 试跑 | 时间 | 死在哪 | 诊断 | 处置 |
|---|---|---|---|---|
| 1 | 10:18 | clip_grad_norm,7 秒崩 | NCCL 不收 CPU 张量 | 补丁 2 |
| 2 | 10:27 | muon all_gather,7 秒崩 | 同上,第二处 | 补丁 3 |
| 3 | 10:35 | 不崩,卡在 Step 0 十几分钟 | CPU 上算 NS,10-20 分钟/步 | 补丁 4(SIGKILL 终止) |
| 4 | 11:06 | **跑完 200 步,50:36** | 四项判定全过(见第五章) | **通过,转正跑** |

---

## 三、配置与启动

### 3.1 h1_v4_full.yaml 相对 w3 的差异(其余逐字相同)

1. `model_path` → 官方底座(冷启动)
2. `output_dir` → `output/h1_v4_full`
3. `train_expert_only: false`(★核心:解冻整个 VLM)
4. `lr 5.0e-5` + `vit_lr 1.0e-6`(全模型压低学习率)
5. `enable_fsdp_offload: true`(权重/优化器驻 CPU 按需上卡)
6. `enable_full_shard: true`(offload 需要前向后释放 unsharded 参数)
7. `micro 1 / 累积 1 / global 1`(offload 下保守起步)
8. `max_steps 20000 / save_steps 10000`(两档 ≈54G,磁盘余量充足)

### 3.2 每步时间预算(补丁 4 后)

| 环节 | 在哪 | 耗时 |
|---|---|---|
| 参数搬上显卡 | 内存→GPU | ~1s |
| 前向+反向 | GPU | ~7s |
| 梯度搬回内存 | GPU→内存 | ~1s |
| 动量更新+堆叠 | CPU | ~3-5s |
| **NS 正交化** | **GPU(补丁4)** | **~2s** |
| 优化器收尾(AdamW 1D/embed 在 CPU) | CPU | ~2s |
| **合计** | | **实测 15.18 s/步(热身后 ~14.8s)** |

**实测(试跑 4,200 步全程 50:36)**:20000 步 ≈ 84 小时 ≈ **3.5 天**;峰值显存仅 **12.32G/32G**。

### 3.3 命令

```bash
# 200 步试跑(独立目录,跑完即删)
python mjq_lingbotvla_v2/Lingbot_H1_Training_Full.py \
    --train.max_steps=200 --train.save_steps=100 \
    --train.output_dir=output/h1_v4_full_TRIAL

# 正跑 20000 步(★由用户亲自启动)
python mjq_lingbotvla_v2/Lingbot_H1_Training_Full.py

# 中断重启:直接再跑(自动找 output/h1_v4_full 最新档续训)
```

---

## 四、复原(方案 B 不行时,三条命令全部恢复原样)

```bash
cp mjq_lingbotvla_v2/B_patch/torch_parallelize.py.orig lingbotvla/distributed/torch_parallelize.py
cp mjq_lingbotvla_v2/B_patch/train_lingbotvla.py.orig tasks/vla/train_lingbotvla.py
cp mjq_lingbotvla_v2/B_patch/muon.py.orig lingbotvla/optim/muon.py
```

自检(三条 diff 均无输出 = 已复原):

```bash
diff mjq_lingbotvla_v2/B_patch/torch_parallelize.py.orig lingbotvla/distributed/torch_parallelize.py && \
diff mjq_lingbotvla_v2/B_patch/train_lingbotvla.py.orig tasks/vla/train_lingbotvla.py && \
diff mjq_lingbotvla_v2/B_patch/muon.py.orig lingbotvla/optim/muon.py && echo 已全部复原
```

---

## 五、试跑判定标准(四项)

1. **不 OOM**(200 步全程显存不爆)
2. **真实 it/s**:tqdm 显示 15-25s/步为达标;>1 分钟/步需重新评估
3. **loss 正常**(下降,无 NaN/爆炸)
4. **step 100 DCP 落档**(offload 后检查点写入是最后一个未验证环节)

- 四项全过 → 用户亲自启动 20000 步正跑;前 2-4k 步内做 home 探针,若全模型有效,
  预期发起幅值冲出 w3 的 4-6cm 波段
- 任何一项不过 → 复原补丁,转租 4×A100(python 3.10/3.12 + torch 2.8.0+cu128 +
  flash-attn 2.8.3 + transformers 4.57.3,版本清单已备)

### 试跑 4 判定结果(2026-09-04 11:57,**四项全过,通过**)

| 项 | 结果 | 证据 |
|---|---|---|
| ① 不 OOM | ✓ | 峰值显存 12.32G / 32G(余量充足) |
| ② 真实速度 | ✓ | **15.18 s/it**(热身后 14.8s)→ 20000 步 ≈ 84h ≈ 3.5 天 |
| ③ loss 正常 | ✓ | 200 点无 NaN/Inf,量级 0.8-2.0;gbs=1 单样本噪声大,200 步不看趋势;**权重实锤在更新**:step100 vs step200 比对,state_proj Δ=4e-3、VLM q_proj Δ=4e-3、专家 Δ=1.8e-2(日志里 grad_norm/lr 显示 0 是记录通道显示问题,非真 0) |
| ④ DCP 落档 | ✓ | step100/200 各 36G 写入成功,HF 导出 3/3(eval_failed 3 = 未配评估器,无害) |

---

## 六、20000 步结果与续训决定(2026-09-08)

### 6.1 训练完成

20000 步全程 **83h51m(15.09s/it)**,峰值显存 12.32G,9/9 落档;loss 1.31→0.22,**结束时仍在下降,无平台**。

### 6.2 探针 + 闭环结果(详见《V4闭环失败诊断.md》)

| 环节 | B@20000 | 对照 w3@152k |
|---|---|---|
| 发起(抬臂) | ✓ **双臂**(用户肉眼确认) | 从未(幅值钉 11-20%) |
| 趋近方块 | ✓ 双臂有趋势,右臂最近 0.142m | 无 |
| 定位/抓/放 | ✗ 停在上前方 10-14cm,不修正,方块未动 | ✗ |

闭环成功率 0%,但**首次出现"发起+趋近"** —— B 方向性成立,学到一半。

### 6.3 续训到 40000 的依据

1. loss 未饱和;chunk 内抖动 21.4→11.0 单调降;发起幅值带在抬升(最大 16.1cm,w3 从未>6)
2. 样本量:gbs=1 × 20000 = 2 万,仅外部成功复现(5 万)的 40%;40k 步补到 80%
3. 官方 `real_robot.yaml` 默认即 max_steps 40000
4. 磁盘:已删 7 个中间档(252G)+TRIAL(71G),余 562G,足够 8 个新档(288G)

### 6.4 续训操作

配置已改 `max_steps: 40000`,启动命令不变(自动从 global_step_20000 接上):

```bash
python mjq_lingbotvla_v2/Lingbot_H1_Training_Full.py
```

★启动后第一件事:看进度条是否显示 **20000/40000**(接上了);若显示 **0/40000** 立即停——那是没读档、从头冷启动。

## 七、40000 步结果:抓取映射学会,闭环缺口=最后几厘米(2026-09-11)

### 7.1 训练

76h07m(13.70s/it),loss 0.216→0.182 未饱和,0 NaN,峰值显存 12.32G。

### 7.2 闭环 0/10,但距离在收敛

| 指标 | 20000 | 40000 |
|---|---|---|
| 左指尖最近(基线) | 0.191m | **0.105m**(5/10 ≤9cm) |
| 右指尖最近(基线) | — | 0.159m |
| E 组(gain1.2+reinfer25) | — | L 0.074 / R 0.108,最佳 |
| 爪相位 | — | 左爪贴近 6cm 恒开 0.94;右爪 9-15cm 空中狂闭 |

### 7.3 双探针判决(详见《V4闭环失败诊断.md》)

- **教师强制**(`grip_teacher_forced_probe.py`):演示观测上,双爪锚点处 100% 正确闭合且分侧,另一侧恒开 → **抓取映射已学会**,不是"不会闭"
- **演示重放**(`grip_replay_probe.py`):闭环环境开环重放演示动作(保真 Δjoint 0.001rad),用我们自己的渲染喂模型,锚点闭合率≈教师强制 → **渲染域无罪**
- 结论:闭环 0% = **趋近段停短**。距地板标尺:演示抓取瞬间指环中点-块心 ≈5.6cm,闭环 6.2-8.8cm 离地板仅 1-3cm,但**偏后 5-9cm**——方块从未进入指间抓取构型,左爪"贴块"视觉条件从未出现;右爪+模型提前 ~1s 规划闭爪 → 空中狂闭

### 7.4 续训到 60000

loss 未饱和 + 缺口是精度而非能力 → 加样本是正解。已删中间档 22500-37500(7×36G),`max_steps: 60000`,启动后确认进度条 **40000/60000**。

## 八、60000 步结果与判词:加步数这条路到头了(2026-09-15)

### 8.1 训练

76h10m(13.71s/it),loss 0.181→**0.162**,末两桶 0.1621/0.1621 → **平台**。0 NaN。

### 8.2 三段趋势:趋近收益指数衰减,行为钉死

| 指标 | 20000 | 40000 | 60000 |
|---|---|---|---|
| loss(末桶) | 0.22 | 0.182 | 0.162(平台) |
| 左指尖最近(基线) | 0.191 | 0.105 | **0.086** |
| 右指尖最近(基线) | — | 0.159 | 0.162(平) |
| E 组 L / R | — | 0.074 / 0.108 | 0.0775 / 0.108(双平) |
| 抓取成功 | 0/10 | 0/10 | **0/10 × 2 配置** |
| home 探针 L 超前 | 0.18 | 0.18 | 0.18(钉死) |

E 组 7.75cm,距 5.6cm 地板仅剩 2.2cm,但 40k→60k 一厘米都没再啃动。

### 8.3 爪相位新行为(方向对,量不够)

- 基线组:**首次出现贴块闭合**——5 回合到 <8cm,其中 1 回合左爪真闭(cmd −0.35);但停位仍偏后,闭了也是抓空
- 左爪开始在 13cm 空中试闭(125 帧)——闭环触发链在松动(40k 恒开 0.94)
- 右爪仍空中狂闭(闭命令帧中位距离 0.35-0.45m)

### 8.4 判词

**60k 样本(=外部成功复现 1.5 倍当量)仍 0 抓取,loss 平台 + 行为钉死 → gbs1 纯加步数已榨干**。剩余缺口是"最后 5cm 对位修正"这一闭环技能,不是映射、不是渲染域、不是样本量。下一步选项(租 A100 gbs4 / v3 末段窗口 / oracle 闭爪定界实验)见对话记录,待拍板。

## 九、定界实验 C:重放交棒(2026-09-15)——抓后段大半已会,墙=抓取构型本身

### 9.1 设计(mjq_lingbotvla_v2/rollout_oracle_handoff.py)

回答一个定界问题:**假设抓取真的发生了,后面的提/搬/放模型会不会?**
闭环环境开环重放演示动作到 F 帧(保真 0.001rad,抓取物理真实发生,无焊接作弊),
然后 policy.reset 交棒,模型接管,按闭环 rollout 同款执行口径(默认 E 配置
gain1.2+reinfer25)。F = 闭爪锚点 + offset:负=轨迹内接管,正=抓后接管。
对照组 force_close=原 C 字面设计(贴块 8cm 强制闭爪)。
网格:R 臂 6 回合×{−120,−60,−15,+15,+60},L 臂 6×{+15,+60},对照 5 回合。

### 9.2 结果(@60000,42 run)

| 接管点 | 指距min | 爪cmd min | 块提起>3cm | 块-桶xy min | 入桶 |
|---|---|---|---|---|---|
| R −120/−60/−15(18 run) | 0.049-0.063 | −0.46~+0.18 | **0/18** | 0.304(块没动) | 0 |
| R +15 | 0.055 | −0.62 | **5/6** | 0.100 | 0 |
| R +60 | 0.057 | −0.59 | **6/6** | **0.074**(ep255 达 0.007) | **1/6** |
| L +15 | 0.057 | −0.68 | 4/6 | 0.139 | 0 |
| L +60 | 0.058 | −0.70 | **6/6** | **0.058** | 0 |
| force_close(5 run) | 0.197 | +0.59(阈值没触发过) | 0/5 | 0.304 | 0 |

帧级机制:
- **轨迹内接管(负 offset)**:模型从完美演示状态接管后,要么 1s 内就空中闭爪
  (闭时指距 7-33cm!)要么贴到 5.7cm 地板悬停不闭,然后**后撤**(指距末值
  涨到 0.29-0.42)——趋近末段+抓取构型这个技能本身缺失,不是早期漂移累积
- **抓后接管(正 offset)**:握紧(压到负饱和)→提升 20-45cm →**横搬到桶边
  4-15cm(本项目首次看到方块被搬运)**→ 在距桶心 5-20cm 处松爪/掉落,仅
  ep255 R+60 真正入桶(7mm 精度!)。L 臂搬运一致性极佳(6/6 都到 4.6-6.8cm)
- **force_close 对照**:E 配置下悬停 11-23cm,8cm 闭爪阈值从未触发——执行侧
  "贴近就闭"小hack无料可用

### 9.3 定界结论

1. **技能分解**:趋近到附近✓ / 末段下降+捕获✗(唯一硬墙,轨迹内接管 0/18)/
   握持+提升+搬运✓(抓后接管 11/12)/ 桶边精确放置✗(差 5-15cm,容差 3cm,1/24 入桶)
2. **末几厘米病在两处复发**:方块前(捕获)和桶前(放置)——同一根因的两个症状,
   修末段的方案必须两段都覆盖;只修抓取,成功率天花板 ~10-20%
3. **修好抓取→搬运立即兑现**(块首次被搬到桶边);钱花在末段精度上有真实回报
4. 执行侧补丁救不了(悬停距离连 hack 阈值都够不着)→ 只能训练侧解决

## 十、执行侧 Ruckig 平滑层(2026-09-15):抖动已治,决策不受影响

用户观看 60k 闭环时发现全程推理抖得厉害(分段交棒反而顺)。根因早已定位:
模型 chunk 首帧命令超前当前状态 0.16-0.49 rad(数采口径中位仅 0.024),
每 50 帧块边界 teleport 一次。按用户要求把推理动作先过运动规划模块再执行。

落地:`Lingbot_H1_Rollout.py --smooth ruckig`(`RuckigSmoother`,Ruckig 0.19.x
流式接口 update+pass_to_input;限值=数采 p99×2-3:臂 v2.0/a20/j200,爪 v4.0/a40/j400;
增益/限幅之后的最终输出过在线轨迹生成,每帧重规划不停顿)。

A/B 实测(@60000,各 2 回合):

| 指标 | off | ruckig |
|---|---|---|
| 臂命令帧间\|Δ\| p99 | 0.223-0.245 | **0.052**(数采口径 0.02 级) |
| max | 0.33-0.42 | **0.058-0.064**(被 v2.0/30=0.067 限住) |
| 指端-块最近距离 | 持平(±1cm 噪声级) | 持平 |
| 抓取 | 0 | 0(预期:平滑不产生技能) |

判读:teleport 消除,担心的"限速拖慢导致更够不着"没有发生;但捕获墙(C 实验
0/18)是决策层缺技能,平滑层治表现不治决策(方案文档 §19)。执行侧到此收工,
训练侧 A/B 决策(租 A100 gbs4 / v3 双末程窗口)不受影响。
