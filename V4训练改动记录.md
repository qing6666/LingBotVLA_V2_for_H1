# V4 训练改动记录 —— 2026-08-27

> 前提：v4 数采收官 302 回合全量 QC 通过（结构/NaN/域/夹爪沿/风格/loader 全绿；
> ep213"双闭合"为 0.6 阈值抖动误报，保留）。根因分析与协议背景见《V3踩坑记录.md》。
> 本文档记录：为训 v4 做的**全部改动**（数据准备 / 代码 / 配置 / 命令），
> 以及过程中踩的两个坑（头部 std=0 NaN、权重向量少写一维）。

---

## 一、数据准备（三步，原数据全程只读）

### 1. 裁剪副本
`mjq_lingbotvla_v2/trim_idle_prefix.py` 配置区改指 v4：
- SRC → `data/h1_build_pick_v4`，DST → `h1_build_pick_v4_trimmed`
- `DROP_EPISODES = set()`（v4 QC 无人工剔除；v3 时是 {30,33,36,37,100,141}）
- 结果：**302 episodes / 460,499 帧**（裁开头静止前缀 14,162 帧 = 3.0%，
  onset 中位 43 帧，无死回合），脚本自校验通过，mp4 零重编码。

### 2. norm 统计
新建 `configs/vla/norm_compute/h1_v4_norm.yaml`（复制 h1_norm.yaml，train_path 指副本）：
```bash
CUDA_VISIBLE_DEVICES=0 bash train.sh scripts/compute_norm_stats.py \
    configs/vla/norm_compute/h1_v4_norm.yaml \
    --data.data_name h1 \
    --data.train_path .../data/h1_build_pick_v4_trimmed \
    --data.norm_path assets/norm_stats/h1_v4.json
```
（46 万帧 ≈ 80 分钟，~95 batch/s。）
（2026-08-27 补：以上 CLI 已包成启动器 `mjq_lingbotvla_v2/Lingbot_H1_NormStats.py`，
纯 Python 进程内 runpy 调用，自带路径前置检查 + 算完自动体检恒定维 std<1e-3；
`--dry-run` 只看参数不真算。换新批次数据时改它配置区四处即可。）

### 3. ★坑：头部 std = 0 → 0/0 = NaN（不改必死）
v4 头部完全固定，实测 `action.head_j1 std = 0.0000`（精确恒 0）、
`Head_j2 std ≈ 1e-3`（浮点抖动）。而 head 槽位是真实映射 + meanstd，
`RunningStats` 的 std **没有下限保护**（lingbotvla/utils/normalize.py:127 只防负方差）：
- yaw 维：(x−mean)/std = **0/0 = NaN**，动作目标全 NaN，训练直接报废；
- pitch 维：1e-3 抖动被除成 ±1 随机噪声。

**对策**：`mjq_lingbotvla_v2/fix_v4_head_std.py` 给头部 4 处 std（state/action ×
yaw/pitch）打 **0.02 下限**。效果：头部归一化值恒 ≈0；推理反归一化时即使模型
预测偏 ±0.5，物理误差也只有 ±0.01 rad——**头部由数据协议锁定，rollout 零改动**。

> 为什么不能像腰部那样直接取消映射？腰是"声明不映射"（槽位零填充即可）；
> 头部若取消映射，rollout 反查 55 维输出时头部维变 0（yaw=0/pitch=0 看向水平），
> 与训练视角（0.53 俯视）形成域差距。同一个坑，头部必须换解法。

---

## 二、代码改动：per-dim 损失加权（3 处，缺一不可）

新增配置字段 `action_loss_weights`（长度 = max_action_dim = 55 的逐维权重，
`None` = 不加权 = **与历史行为逐比特一致**）。三处都要改的原因：

| 文件 | 改动 | 为什么必须有 |
|---|---|---|
| `lingbotvla/utils/arguments.py` | TrainingArguments 加字段 | yaml 新键会被 `parse_known_args` 的 remaining_args 检查直接 ValueError |
| `lingbotvla/models/vla/lingbot_vla/configuration_lingbot_vla.py` | 基类 `__init__` 显式收参 + 赋值 | 基类的 `**kwargs` 传给 `super().__init__()` 时被丢弃，不显式声明字段到不了 config |
| `lingbotvla/models/vla/lingbot_vla/modeling_lingbot_vla_v2.py` | ① policy `__init__` 注册权重 buffer；② `forward` 在归约前乘 losses | 实际生效点 |

设计决策：
- buffer 用 **persistent=False** → 不进 state_dict/checkpoint，旧权重加载、
  推理、deploy 完全不受影响；
- 权重乘在 `self.model.forward` 返回的逐元素 losses 上、**joint_mask/归约之前**
  → 加权后的 `loss_vla` 正确；MoE 均衡损失（seq_wise/router_z）在 model.forward
  内部已用**未加权** losses 算好，不受污染；
- 长度断言 `len(w) == max_action_dim`，启动即报错。

### ★坑：手写 55 个数少写了一个
yaml 权重向量初版 54 维（hand+base 段少一个 1.0），人工数了两遍都没数出来，
程序化断言一次就抓到。教训：**长向量必须写脚本断言，不能靠肉眼**。
校验命令见 `h1_v4.yaml` 注释（1.0×44 + 2.0×9 + 0.0×2 = 55）。

### 55 维统一空间下标备忘（写权重/看日志都用）
```
[0:14]  双臂     L臂 0-6, R臂 7-13     ← robot_config origin_keys 顺序
[14:28] 末端位姿  未映射(零填充)
[28:30] 夹爪     Grip.L=28, Grip.R=29
[30:34] 腰       未映射(v4 实测仍静止 std 2e-4)
[34:36] 头       yaw=34, pitch=35      ← v4 恒定,权重 0
[36:39] 底盘     未映射
[39:51] 灵巧手   未映射
[51:55] 尾部补零(prepare_action 补到 max_action_dim)
```

---

## 三、训练配置：configs/vla/real_robot/h1_v4.yaml

在 h1.yaml（v3 同款）基础上**只改 6 处**，其余逐字未动：

| 参数 | v3 | v4 | 依据 |
|---|---|---|---|
| data.train_path | v3_trimmed | h1_build_pick_v4_trimmed | 新数据 |
| data.norm_stats_file | h1_v3.json | h1_v4.json | 头部 mean/std 完全不同，绝不能沿用 |
| train.output_dir | output/h1_v3 | output/h1_v4 | 绝不覆盖旧结果 |
| train.max_steps | 20000 | **40000** | 数据 3.7×；40k×batch4=16 万样本(v3 的 2 倍，≈0.35 epoch)；不够 enable_resume 续 |
| train.save_steps | 2500 | **4000** | 磁盘：每档≈26.6G，10 档≈266G（余 419G）；2500×16 档=426G 会爆盘 |
| train.action_loss_weights | 无 | R臂[7:14]×2 + 夹爪[28:30]×2 + 头[34:36]×0 | v3 实测偏置 R_J1+0.19/R_J4−0.16/Grip.R−0.26 |

**坚决不动**（与预训练权重/32G 显存绑定）：MoE 结构、train_expert_only、
lr 1e-4 constant、L1_fm、micro 1 + accum 4、Muon、bf16、chunk 50、蒸馏关。

吞吐参考：v3 实测 ≈3.5 s/步 → 40k 步 ≈ **39 小时**。

---

## 四、启动与评测命令

```bash
# 训练（启动器已切 v4，端口 29612）
conda activate lingbotv2
python mjq_lingbotvla_v2/Lingbot_H1_Training.py

# 评测（裸跑！无 --bias-comp 无 --action-gain）
python mjq_lingbotvla_v2/Lingbot_H1_Rollout.py --headless --episodes 10 \
    --norm-path assets/norm_stats/h1_v4.json \
    --model-path output/h1_v4/checkpoints/<档>/hf_ckpt
# ★ rollout --norm-path 默认还指 h1_v3.json，评测 v4 必须显式传！
```

## 五、改动文件清单

| 文件 | 性质 |
|---|---|
| `mjq_lingbotvla_v2/trim_idle_prefix.py` | 改（配置区切 v4） |
| `mjq_lingbotvla_v2/fix_v4_head_std.py` | 新建（头部 std 地板） |
| `configs/vla/norm_compute/h1_v4_norm.yaml` | 新建 |
| `configs/vla/real_robot/h1_v4.yaml` | 新建 |
| `mjq_lingbotvla_v2/Lingbot_H1_Training.py` | 改（配置区切 v4） |
| `lingbotvla/utils/arguments.py` | 改（+1 字段） |
| `lingbotvla/models/vla/lingbot_vla/configuration_lingbot_vla.py` | 改（+1 参数/赋值/typing import） |
| `lingbotvla/models/vla/lingbot_vla/modeling_lingbot_vla_v2.py` | 改（buffer 注册 + 加权乘法） |
| `assets/norm_stats/h1_v4.json` | 生成（统计 + std 地板） |
| `data/h1_build_pick_v4_trimmed/` | 生成（裁剪副本，原数据未动） |
